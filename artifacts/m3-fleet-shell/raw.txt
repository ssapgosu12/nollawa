M3-FLEET-SHELL-A raw evidence
baseline: 1a1e75a7c58ca789ba6b8f8791675c5ab4dba1e7
card sha256: d97c60624f24ae5e7644f323ab5fd4b064d64f9dc7e7cae2a95dd1a4f7f67453

Pre-fix causal hypotheses and falsifiable observations
- B-21 hypothesis: App reloads on every service-worker controllerchange because the listener has no explicit-consent gate. Expected: a controller change before confirmation produces zero reloads; confirmation posts ACTIVATE_UPDATE; subsequent controller changes produce exactly one reload.
- B-20 hypothesis: App reuses the same FleetGame component instance when the local fleet actor changes. Expected: the App-to-FleetGame key changes for local actors 1 -> 2 -> 3, remains stable while the actor is unchanged, and a two-player setup can proceed through participant 2 confirmation into placement.

Implementation observations
- B-21: createUpdateConsentController ignores controllerChanged before consent, confirm posts {type: ACTIVATE_UPDATE}, and the first subsequent controllerChanged reloads while later duplicates do not.
- public/sw.js was intentionally unchanged: clients.claim(), cache activation, and ACTIVATE_UPDATE -> skipWaiting remain intact.
- B-20: the App-to-FleetGame callsite now derives key from the local fleet actor; remote identity remains the remote key. The separately named test drives two-player setup through participant 2 confirmation into placement and drives three-player setup across clean local-1, local-2, and local-3 keys.

Automated evidence
- ./node_modules/.bin/vitest run src/m3-fleet-shell.test.ts -> exit 0; 1 file passed; 3 tests passed.
- ./node_modules/.bin/vitest run -> exit 0; 44 files passed; 394 tests passed.
- npm run check:terms -> exit 0; Forbidden-term gate passed: 70 files, 30 terms.
- npm run build -> exit 0; TypeScript no-emit and Vite production build passed; 40 modules transformed.

SHOW-ME attempt and population
- Local preview http://127.0.0.1:4173/ returned HTTP 200 with 654 bytes.
- Browser runtime selection for that URL returned: No browser is available.
- Required troubleshooting discovery returned an empty browser list: total denominator 0; available 0; unavailable requested observations 3; uninspected remainder 0.
- Requested screenshot 1 (participant 2 clean setup): RED, not captured.
- Requested screenshot 2 (participant 2 confirmation followed by placement): RED, not captured.
- Requested screenshot 3 (in-progress game survives simulated deployment with update notice): RED, not captured.
- No screenshot paths were created. Empty or synthetic files were not substituted for browser evidence.

Card population
- denominator: 2 rows, derived from rg -n "impl:FS-SHELL" docs/plan/fleet-strike.md and limited by this card to B-20 and B-21.
- B-20: RED 1, GREEN 0, NOT-YET 0, uninspected 0. Automated behavior is GREEN, but the required browser screenshots are absent, so the row cannot be GREEN.
- B-21: RED 1, GREEN 0, NOT-YET 0, uninspected 0. Automated behavior is GREEN, but the required browser screenshot is absent, so the row cannot be GREEN.
- Physical-device verification: NOT-YET.
