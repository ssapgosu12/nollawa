검수 보고서 §0·§2와 지시 원문, 그리고 지목된 코드 지점을 전부 열어 대조했다. 아래는 그 대조 결과에 기초한 수정 순서다.

---

# 함대 격침 37개 항목 수정 순서

## 대조 중 확인한 순서 관련 정정 3건 (먼저 읽을 것)

1. **D-1이 B-20을 막지 않는다.** D-1의 `blocks`에 B-20이 들어 있지만, B-20의 근본 수정 지점은 `src/App.tsx:607`(FleetGame에 배우 단위 `key` 부여)이고 D-1이 다시 쓰는 `FleetGame.tsx:199`(설정 화면 JSX)와 파일도 줄도 겹치지 않는다. 콜사이트 대증요법(`FleetGame.tsx:150` 근처 리셋)을 택할 때만 겹친다 — 그 경로는 초기화할 지역 상태 7종을 일일이 나열해야 해서 재발 위험이 크므로 어차피 채택하면 안 된다. **B-20을 D-1 뒤로 미룰 이유가 없다.**
2. **D-1이 B-17도 막지 않는다.** B-17의 수정은 `styles.css:103` 한 줄(`.fleet-board.fleet-summary` → `.fleet-summary`)뿐이고 TSX를 건드리지 않는다. D-1과 완전 독립이다. 실제로 D-1에 막히는 것은 `FleetGame.tsx:199` 안에 사는 **B-15와 B-16의 프리셋 카드 설명 부분**뿐이다.
3. **D-2.5의 "해결책이 이미 코드 안에 있다"는 성립하지 않는다.** 재사용 가능한 자산은 `FLEET_SHOT_LABELS`(FleetGame.tsx:42-45) 하나이고 그마저 이름 두 개가 사양과 다르다(`salvo`='일제사격탄'→사양 「연속 사격」, `random-shot`='무작위탄'→사양 「랜덤샷」). 클래식이 멀쩡한 이유는 탄종이 하나뿐이라 `:229`에 문자열을 리터럴로 박았기 때문이다. **"옮기면 되는 항목"이 아니라 "맵 2~3개를 새로 쓰고 6자리에 배선하는 항목"으로 예산을 잡아야 한다.** 다만 이름·설명의 원본이 전부 사양 문서에 있으므로 사용자 결정은 필요 없다.

**추가 관측 (순서에 영향):** `FleetGame.tsx`는 한 줄이 화면 한 구획 전체인 초압축 JSX다(`:199`=설정 화면 전체, `:221`=중단 보너스 버튼 전체, `:229`=하단 영역 전체, `:208-209`=상단 보드 전체). 따라서 **충돌 단위는 "같은 파일"이 아니라 "같은 줄"이다** — 같은 줄을 건드리는 두 항목은 병렬로 돌리면 100% 텍스트 충돌한다. 아래 레인 분리는 이 기준으로 잡았다.

---

## 1단계 — 관측 가능성 회복 (다른 모든 검증의 전제)

| 항목 | 지점 | 이 순서인 이유 |
|---|---|---|
| **B-21** 서비스워커 강제 리로드 | `App.tsx:624`(+`public/sw.js:14`) | 이것이 살아 있으면 다른 32건을 검증하는 도중 판 상태가 계속 날아간다 — 보고서 §4 검수 메모 4번이 기록한 실제 방해 요인이다. 고치는 비용은 동의 플래그 한 줄(S). |
| **B-20** 로컬 모드 2P 진행 불가 | `App.tsx:607`에 배우 단위 `key` | 로컬 모드는 브라우저 탭 N개 없이 한 화면에서 전 항목을 재현하는 유일한 저비용 검증 경로인데 지금 프리셋 단계에서 완전히 막혀 있다. 그리고 11단계(D-3(a))가 같은 좌석 순환 코드를 다시 쓰므로, 깨진 토대 위에 동시화를 얹지 않으려면 여기서 잡아야 한다. |

병렬 가능: 두 항목은 `App.tsx`의 서로 다른 구획(624 vs 607)이라 동시 진행해도 충돌하지 않는다.

---

## 2단계 — 사용자 결정 일괄 발송 (답을 기다리는 동안 3~5단계 진행)

결정은 왕복 지연이 있는 유일한 자원이므로 **가장 먼저 한 번에 던지고, 답이 오는 동안 결정과 무관한 코드를 돌린다.** 물어야 할 것은 실제로 다섯 개뿐이다.

1. **D-2 — 표기 세 가지**: 「밝힘·배 있음」/「밝힘·빈칸」/「격침」을 각각 무엇으로 그릴 것인가. (D-048은 빨강×·검정×·작은 빨강× 셋만 정했다.) ★코드 실측상 "밝혀진 칸"은 한 종류가 아니라 둘이다 — 조명탄이 빈 칸을 밝히면 `fleet.ts:372` 초기값 그대로 `'miss'`가 되어 **실제로 쏴서 빗나간 칸과 구분되지 않는다.** 보고서는 앞의 한 경우만 적었다.
2. **B-19 — 배 꼬리 길이 숫자**: 사양에 없는 요소이므로 지울 것인가, 아니면 연안·원양 꼬리표와 구분되는 다른 표기로 남길 것인가. 지우면 클래식에서 "어느 배가 어디 놓였는지" 읽을 단서가 사라져 B-24가 조금 나빠진다.
3. **B-22 — 배치 미리보기 확정 수단**: 세로 폰 기준(D-045)이라 hover를 쓸 수 없다. 「첫 탭=미리보기, 둘째 탭=확정」인가, 중단의 기존 확인 버튼 재사용인가.
4. **B-23 — 회전 기준점**: 현재는 `ship.cells[0]`(변형 2차원 배에서는 외접 사각형 좌상단) 고정이다. 중앙 기준으로 바꿀 것인가, 기준점은 두고 미리보기로 보여줄 것인가. 중앙 기준을 고르면 판 경계 클램프 규칙까지 정해야 한다.
5. **B-26 — 종료 표시**: 종료 전용 화면을 낼 것인가, 승/패/무승부 문구를 어디에 어떻게 놓을 것인가.

**D-1과 D-3은 이 목록에 없다.** D-1은 결정이 아니라 문서 반영 누락이고(D-035 뒤 문단 `DIRECTIVES.md:578·590·593`이 앞 문단 `:564`의 묶음 프리셋을 이미 대체했다), D-3은 2026-08-15 지시가 내용을 이미 확정했다. 이 둘을 결정 대기로 분류하면 불필요하게 판이 선다.

---

## 3단계 — 사양 문서 확정 (코드 파일 0개 · 어느 레인과도 충돌 없음)

| 항목 | 지점 | 이 순서인 이유 |
|---|---|---|
| **D-1 문서** | `fleet-strike.md:33` 삭제 · `:34` 재작성 · `:30`의 낡은 수치(「3인 이상 9종, 2인 7종」→ §3:60·`fleet.ts:7-9` 기준 8종) 교정 | §2가 배타적인 두 세대를 함께 보유하는 한, 다음 세션이 묶음 프리셋을 다시 만든다. 구현(6단계)의 입력이다. |
| **D-3 문서** | `fleet-strike.md:359-371` §5를 프리셋·배치·사격 전부로 확대 + `:12-24` §1에도 같은 문장 + **로컬 모드 예외 명시** | 규정되지 않은 자리를 구현이 "한 사람씩 돌리기"로 채운 것이 B-30·B-31의 뿌리다. ★반드시 두 가지를 문서에 박을 것: ①**전원 완료 시 자동 전환은 이미 구현돼 있다**(`fleet.ts:252-255`, `:457-460`) — 보고서가 제안한 "보이지 않는 시작 버튼을 1초마다 자동으로 누른다"를 증보문에 적으면 다음 세션이 이미 있는 동작 위에 폴링 타이머를 덧댄다. ②**동시는 방(멀티) 기준이고 로컬은 한 기기이므로 순번으로 대행한다** — 이 문장이 없으면 `setupParticipantId`·`placementParticipantId`를 없애는 순간 `fleetActorId`(`fleet.ts:476-481`)가 돌려줄 행위자를 잃어 로컬 모드가 죽는다. 참고할 형태는 같은 함수 `:480`(살아 있고 아직 확정하지 않은 첫 사람)에 이미 있다. |
| **DIRECTIVES 신규 D항목** | `DIRECTIVES.md`(현재 D-048에서 끝남) | 2026-08-15의 동시화 지시와 (2단계 답이 오면) 표기 결정이 지시 원문에 존재하지 않는다 — 사양 문서만 고치면 근거가 순환한다. |

3단계는 문서만 만지므로 4·5단계와 완전 병렬이다.

---

## 4단계 — 결정·문서와 무관한 단독 결함 (병렬 3레인)

여기 있는 항목들은 서로 다른 파일/줄 구획을 소유하므로 세 레인을 동시에 돌릴 수 있다. 앞 단계에 의존하지 않는 것을 여기 모아 둔 이유는, 2단계 결정 답변을 기다리는 시간을 비워 두지 않기 위해서다.

**레인 A — `styles.css` 단독 소유 구획**
| 항목 | 지점 | 이유 |
|---|---|---|
| **B-1** 후미 텍스처 좌우 반전 | `:116`을 bow/stern으로 분리 · `:118` 반전 | 다른 어떤 항목과도 뿌리를 공유하지 않는 완전 단독 결함이고, 렌더러(`FleetGame.tsx:88`)는 이미 정상이라 CSS 2줄로 세로 배까지 함께 고쳐진다. |
| **B-11** 점수판 손잡이가 게임 틀 이탈 | `.fleet-sheet-handle` 규칙 **신설** | 후크는 이미 있는데(`FleetGame.tsx:232`가 클래스를 넘김) `styles.css`에 그 규칙이 없는 것을 실측 확인했다. 공용 `:84`를 고치면 요트가 깨지므로 전용 재정의만 추가. |
| **B-27** 보드 이름표가 1행을 덮음 | `:97` shell을 이름표 행+보드 행 2행 grid로 · `:101` | B-11과 같은 `:95-105` 구획을 만지므로 **B-11 다음에 같은 사람이** 한다. 또 이름표 행 확보가 보드 높이를 줄이므로, 상단에 보드를 세 장 그리는 9단계(B-13) 전에 확정돼 있어야 한다. |
| **B-17** 안내 두 문구가 붙음 | `:103` | `.fleet-board.fleet-summary` → `.fleet-summary` 한 줄. D-1과 무관함을 확인했다(위 정정 2). |
| **B-18** 버튼 글자 단어 중간 줄바꿈 | `:127-128` | 중단 버튼 수는 이후 단계에서 **늘어난다**(B-6의 가로/세로 토글, B-33의 택1 두 박스, B-7의 안내 슬롯). 넘침 정책을 지금 깔아 두지 않으면 그 단계마다 같은 증상을 다시 본다. |
| **B-10** 연출 자막이 상단 보드를 덮음 | `:138-139` + `FleetGame.tsx:238` | `:238`은 파일 마지막 줄이라 다른 어떤 항목과도 겹치지 않는다. 10단계(B-9)가 자막 노출 시간을 크게 줄이므로 그 전에 두면 체감 검증이 쉽다. |

**레인 B — `fleet.ts` 리듀서 단독 구획**
| 항목 | 지점 | 이유 |
|---|---|---|
| **B-2** 예광탄 가장자리 전체 무효 | `:337-338` rangePlan에 tracer 포함 + 중심 칸 `validCell` 별도 검사 | 실측 확인: `if (impacts.length === 0 && !rangePlan) return state;`가 rangePlan 쪽의 유일한 하한이므로, tracer를 넣기만 하고 중심 검사를 안 넣으면 **판 밖 중심 예광탄이 impacts 0개로 통과한다.** 두 줄은 반드시 한 커밋. |
| **B-3** 압박 고폭탄이 항상 조명탄 | `fleet.ts:314` + `FleetGame.tsx:221` 패리티 동시 반전 | 실측 확인: `roundIndex % 2 === 1`(리듀서)과 `round % 2 === 0`(UI)은 `round = roundIndex+1`이라 **현재 서로 일치한다** — 즉 한쪽만 뒤집으면 버튼은 뜨는데 리듀서가 거부하는 새 무반응이 생긴다. B-2와 같은 함수 `queueVariantShot` 안이라 같은 레인에 둔다. |
| **B-8** 연안·원양이 언제나 이웃 두 척 | `:433-441` + `:182` | 완전 독립. ★수정 후 회귀 확인 때 보고서의 「검수 3명 전원 동일 태그」를 따로 봐야 한다 — `tagOffset`은 참가자마다 `Math.random`에서 독립으로 뽑히므로(`App.tsx:431`이 random 인자 미전달) 전원 동일은 약 1/343이고, 구조 결함(이웃 고정·뒤쪽 배 제외)과 별개의 미설명 원인일 수 있다. |

**레인 C — `App.tsx` 화면 전환**
| 항목 | 지점 | 이유 |
|---|---|---|
| **B-28** 클래식 뒤 변형이 옛 화면 | `:272` · `:325` · `:234` | 1단계(B-21)가 `App.tsx:624`를 먼저 만지므로 그 뒤. ★보고서가 지목한 `:426/:434`는 원인이 아님을 확인했다(로컬 시작 경로는 `setFleetState(next)`로 덮어쓰고 `selectedGame`은 `:607` 조건에 쓰이지 않는다). **재현 먼저**: 방 메시지가 새 스냅샷보다 먼저 도착하는 창이 기전이라면 방(멀티) 모드·비방장에서 났을 것이다. 확인 없이 고치면 엉뚱한 곳을 만진다. |

---

## 5단계 — 설정 화면 재작성 체인 (전부 `FleetGame.tsx:198-199` 한 줄 · 직렬 필수)

이 세 항목은 물리적으로 같은 줄에 산다. 병렬로 나누면 무조건 충돌하고, 순서를 뒤집으면 두 번 고친다.

1. **D-1 구현** — `fleet.ts:22-26`(`specialShipOffers` 필드가 결합의 자료구조적 실체) · `:156-165` · `:180` · `:448-451` · `FleetGame.tsx:198-199`. 묶음을 풀어 특수 배 후보를 한 벌만 뽑으면 `(presetIndex*4+offset)%7` 슬라이스 자체가 사라지므로 **보고서가 「추가 문제」로 적은 1번·3번 카드 후보 3장 겹침이 별도 수정 없이 함께 없어진다.**
2. **B-15** 고른 카드가 안 보임 — `:199` 프리셋 버튼에 선택 클래스·`aria-pressed` 추가 + `styles.css:133`의 `.fleet-ship-picker` 한정 `selected` 규칙을 `.fleet-screen` 범위로 승격. 승격 쪽이 본체다: 지금 `selected` 클래스를 제대로 붙이는 특수 배 버튼과 산탄 토글이 **규칙이 없어서** 안 보이는 것이라, 이 한 번의 승격이 8단계 B-33의 절반과 8·9단계의 "내가 뭘 골랐는지 안 보임"을 함께 없앤다. 색은 D-048이 노랑(`--accent`)으로 못박아 새 결정이 없다.
3. **B-16/B-14의 `:199` 부분** — 프리셋 카드 3장 각각의 이름·설명. D-1이 다시 쓴 새 화면 위에서 한 번만 배선한다.

---

## 6단계 — 어휘층 배선 (D-2.5 = B-14 + B-16 · 하나의 작업)

`FleetGame.tsx:42-45`의 `FLEET_SHOT_LABELS` 이름 교정(`salvo`→「연속 사격」, `random-shot`→「랜덤샷」) + 특수 배 7종 맵 + 청사진 id 약 11종 맵을 신설하고, 남은 다섯 자리 `:207` · `:215` · `:219` · `:229` · `:137`(aria)에 배선한다.

- **이 순서인 이유**: 5단계가 `:199`를 다시 쓰므로 그 자리는 이미 끝나 있고, 나머지 다섯 자리는 5단계와 독립이라 여기서 한 번에 끝난다. 그리고 **이 어휘층이 7단계(B-12 상단 중앙 셋째 줄)와 13단계(B-26)의 입력**이므로 그보다 먼저여야 한다.
- 함께 처리할 잠복 결함: `:137`과 `:215`의 aria-label이 변형 함대에도 5개짜리 `CLASSIC_FLEET_NAMES`를 인덱스로 쓴다 — 변형 index 4는 4칸 배인데 「5칸 함선」으로 읽히고, 6번째 이후는 「7번 함선」 폴백이 된다. 화면에 안 보여서 검수에서 잡히지 않았다.

---

## 7단계 — 표기 어휘 확장 (2단계 D-2 답변 도착 후 · 한 커밋)

**B-4 + B-5 + B-25를 하나로 묶는다.** 셋 다 같은 파이프라인(`fleet.ts:20` 타입 → `:383-384` 접기 → `FleetGame.tsx:29`/`:71-74` 접기 → `styles.css:120-123` 클래스)을 통과하므로 따로 잡으면 같은 곳을 세 번 만진다.

- `fleet.ts:20` `FleetShotResult`에 `'revealed'` 추가 · `:383-384` 접기 제거 · `FleetGame.tsx:29`·`:71-74`(여기서 `sunk`→`hit` 접기도 함께 제거) · `:128`·`:134`(배 칸 자체의 격침 상태) · `styles.css:120-123`.
- **UI 우회 금지 근거(코드 실측)**: 사격 기록에 `impactKind`가 남으므로 UI만 고쳐도 될 것처럼 보이지만, 조명탄은 종이배에만 실제 피해를 주므로(`fleet-special.ts:116`) `impactKind === 'flare'`인데 진짜 명중인 경우가 존재한다. UI에서 `impactKind`만 보고 판정하면 종이배 피격을 「밝혀졌을 뿐」으로 오표기한다. 타입을 넓혀 끝까지 살려 보내는 것이 옳다.
- **★반드시 함께 고칠 숨은 곳**: `FleetGame.tsx:51-52` 점수판이 `result !== 'miss'`를 명중으로 세므로, 지금은 조명탄에 드러나기만 한 칸도 "부서진 칸"으로 계산해 남은 배 수를 적게 표시한다. 타입만 넓히고 여기를 안 고치면 점수판이 조용히 틀린 채로 남는다.
- 클래식까지 배 단위로 표현하려면 `fleet.ts:224-227`(클래식 배에 `sunk` 필드 미생성)에 데이터 변경이 따라온다. `:265-270`의 hits 계산이 이미 그 정보를 알고 있으므로 저장만 하면 된다.

---

## 8단계 — 조준 입력 체인 (`:93-110`/`:160`/`:175`/`:208-209`/`:221` 공유 · 직렬 필수)

1. **B-6** 관통탄을 「칸 2개 고르기」에서 사양대로 「가로 1×2 / 세로 2×1 택1」로 되돌린다 (`:159`에서 selectionNeeded 경로 제거 · `:175` · `:99` · `:221`에 토글). 플래너(`fleet-shots.ts:232-239`)는 손대지 않는다. **먼저 하는 이유**: 이 경로가 없어지면 B-32의 관통탄 절반과 B-7의 사례 ②가 통째로 사라진다 — 순서를 뒤집으면 곧 삭제될 코드를 고치는 셈이다.
2. **B-33** 「일반탄 대체」 토글을 D-043의 「박스 1개 = 발사 1번」 택1 표기로. B-6이 만든 가로/세로 택1과 **같은 표기 규칙**이므로 연달아 하면 택1 어휘가 한 번에 통일되고, 둘 다 `:221`이라 어차피 직렬이다.
3. **B-32** 정원 초과 유령 칸 — `:175` 조용한 무시 · `:209` 콤마 연산자가 거부와 무관하게 `targetPreview`를 갱신 · `:160` 정원 충족 후 preview 노출. B-6 뒤에는 조명탄만 남는다. 최소 수정은 S지만, **확정 선택과 미리보기를 서로 다른 표기로 분리하는 쪽(M)을 택해야** 다음 항목이 얹힐 자리가 생긴다.
4. **B-29** 범위탄 착탄 미리보기 — `:177-190`에 부수효과로만 존재하는 계획 산출을 렌더 시점 파생값으로 끌어올린다. **B-32가 만든 미리보기 채널 위에 얹히므로 그 뒤.** 함정 둘: ①무작위탄·산탄은 계획에 `Math.random`이 들어가(`:103-108`) 미리보기와 실제 착탄이 달라진다 — 시드 고정 또는 그 두 탄종은 범위 대신 성격만 표기. ②`shiftRange`(`:176`)가 중심을 판 밖 ±2까지 밀 수 있어 중심이 판 밖이면 아무 칸도 강조되지 않는다.
5. **B-7** 거부 안내 4곳 — 중단 바(`:213-226`) 또는 하단 슬롯(`:229`)에 안내 자리 신설 + 사전 검증(`:221` 기본 사격 전 보너스 버튼 `disabled`, `:199` 3척째, `:229` 배치 불가 칸, `:189` 거부 시 선택 미삭제). **체인 마지막인 이유**: 사례 ②는 B-6이 없애고, 사례 ③은 B-3이 만진 `:221`, 사례 ④는 D-1이 다시 쓴 `:199`에 있다. 앞 넷이 끝난 뒤라야 남은 것에만 배선한다. 리듀서를 「거부 사유 반환」으로 바꾸는 구조 처방은 바둑·요트까지 파급되므로 범위 밖이다.
- B-2를 4단계에서 이미 고쳤으므로, B-7이 붙는 시점에는 예광탄 가장자리 침묵 거부가 재발해도 최소한 사용자에게 보인다.

---

## 9단계 — 배치 입력 체인 (`FleetBoard`/`:229`/`:215-217` 공유 · 직렬 필수)

1. **B-22** 배치 미리보기 신설 — `:112-138` FleetBoard에 배치용 커서 입력 · `:229` 즉시 확정을 2단계로 분리 · `styles.css:111` 인근에 `.target-selected`와 구분되는 커서 스타일. 사양은 이미 열려 있다(D-048 「현재 배치중인 배 커서를 노랑으로 하는건 괜찮음」). 2단계에서 받은 확정 수단(첫 탭/둘째 탭 vs 버튼)을 적용.
2. **B-23** 회전 기준점 — `fleet.ts:234-242` · `FleetGame.tsx:217`. **B-22 뒤인 이유**: 미리보기가 생기면 "기준점을 어디로 두든 보인다"로 대부분 해소되고, 그때 남는 것만 고치면 된다. 부수 결함 함께: `:217`의 `rotate-ship` 분기가 로컬 `orientation`을 갱신하지 않아 이후 다른 배 방향이 어긋난다. ★`fleet.test.ts:23-24`가 현 동작(2칸 배 (0,0) 가로→세로 = (0,0),(1,0))을 단언으로 못 박고 있으므로 함께 고쳐야 한다.
3. **B-24** 배치 완료 여부 표시 — `:215-216` · `styles.css:129-133`. 데이터는 이미 있다(`:161 selectedPlaced`가 계산돼 `:217`에서만 쓰인다). **5단계(B-15의 `selected` 규칙 승격)와 6단계(`:215` 이름 배선) 뒤인 이유**: 같은 버튼의 class와 같은 CSS 블록을 만지고, 노랑=하이라이트 전용(D-048)과 겹치지 않는 표기를 골라야 하므로 선택 표기가 확정된 뒤라야 한다.
4. **B-19** 꼬리표 길이 숫자 — `:137`. 2단계 답변대로. **FleetBoard를 만지는 7·9단계가 끝난 뒤**에 같은 줄에서 한 번에 정리한다(6단계의 aria 정리와도 같은 줄이다).

---

## 10단계 — 상단 구획 (`FleetGame.tsx:204·211` = 두 줄 · 한 티켓)

**B-12 + B-13을 하나로 묶는다.** 둘은 문자 그대로 같은 두 줄의 같은 슬롯이고 같은 조건식을 다시 쓴다.

- B-12(1): `carousel` 조건에 `state.phase === 'targeting'` 게이트 추가 — 사양은 캐로셀을 조준 페이즈의 물건으로 정의하고 배치 페이즈 상단 좌·우는 공란으로 못박았다(D-045, `fleet-strike.md:301·316-317`).
- B-13: 좌·우 15% 슬롯에 이전/다음 대상의 비대화형 `FleetBoard` + `.fleet-board-name` 실제 렌더(`styles.css:125-126·136`도 버튼 중앙정렬에서 축소 보드 배치로). 빠진 것은 화살표 동작이 아니라 「보드를 그린다」는 요구 자체다(D-043, `DIRECTIVES.md:766`). 탈락자 보드를 남기는 부분(D-044 2번)은 `targets`(`:153`)가 이미 거르지 않으므로 맞다.
- B-12(2): `:206-207` 상단 중앙을 「내 함대 현황 + 내 탄종 특성」으로 교체. **셋째 줄은 6단계 어휘층을 소비하고, 첫 줄이 남의 이름인 것은 11단계(동시화)가 없애므로 그 뒤여야 한다** — 순차 진행이 남아 있는 동안 고치면 곧 다시 고친다.
- 4단계 B-27(이름표 행 확보)이 보드 높이를 바꾸므로 그 뒤여야 한다. B-24와 정보가 겹치므로(상단 중앙 vs 중단 배 선택기) 무엇을 어디에 둘지 여기서 한 번 정한다.

---

## 11단계 — 연출 단위화 (B-9)

`fleet.ts:69` impact에 발 식별자 추가 · `:342` 발 단위 보존 · `:366-391` 큐를 발 단위로(캡션 1 + 결과 요약 1) · `:394-398` 연출 종료 전 입력 미개방 · `FleetGame.tsx:158`·`:168-173`.

- **두 증상(칸 단위 재생 · 연출 중 입력 개방)은 한 뿌리이므로 티켓을 쪼개면 두 번 만진다.**
- **12단계보다 먼저인 이유**: 12단계는 클래식을 `resolveVariantRound`에 태우는데, 그 함수의 `:366-391`이 바로 이 항목이 다시 쓰는 자리다. 연출 단위와 입력 게이트를 먼저 세워 두면 클래식이 그것을 그대로 물려받고, 순서를 뒤집으면 B-9가 클래식 몫까지 다시 처리해야 한다.

---

## 12단계 — 동시화 (a) 프리셋·배치 (D-3(a) = B-30)

`fleet.ts:212` · `:244-256` · `:412-413` · `:443-444` · `:457-460` · `:476-481` · `FleetGame.tsx:156`·`:198` · `App.tsx:607` · `fleet.test.ts:112`.

- **먼저 (a)를 하는 이유**: 게이트 개념을 「단일 행위자 토큰 비교」에서 「내 완료 플래그가 아직 false인가」로 바꾸는 같은 변경인데, **배치·프리셋 쪽은 판정 로직이 전혀 없어 개념만 검증할 수 있다.** 사격 쪽은 판정 동시화가 얽혀 있어 개념 오류와 판정 오류가 섞인다.
- 필요한 필드는 이미 있다(`FleetParticipant.placementComplete` `fleet.ts:53`, `variantSetup.complete` `:33`), 전원 완료 시 자동 전환도 이미 있다(`:252-255`, `:457-460`). **지울 것은 토큰뿐**이다.
- 로컬 모드는 3단계 문서가 정한 대로 `fleetActorId:480`과 같은 규칙(살아 있고 아직 마치지 않은 첫 사람)을 설정·배치에도 넣는다. 1단계 B-20이 끝나 있어야 이 코드 위에서 작업할 수 있다.
- 원격 모드는 `App.tsx:276-281`이 보낸 사람 id를 그대로 리듀서에 넘기므로 서버 쪽 추가 게이트는 없다.

---

## 13단계 — 동시화 (b) 클래식 사격 (D-3(b) = B-31)

`fleet.ts:137-143`(`createFleetState`가 `round`·`roundPlans`·`presentationQueue`를 아예 만들지 않는다) · `:260-278` · `:303-344`·`:401-410`·`:362-399` 재사용 · `FleetGame.tsx:158`·`:179` · `fleet.test.ts:34-35`.

- **가장 마지막인 이유**: 사실상 클래식 사격 경로 재작성(L)이고, 앞 단계 전부가 이 경로 위에서 검증되기 때문이다.
- **확인된 함정(반드시 함께)**: `resolveVariantRound`는 `blueprint`가 있을 때만 피해를 적용하고(`:375-385 if (blueprint)`), 클래식 배는 blueprint가 없어 **전부 miss로 기록된다.** 클래식을 그대로 태우면 모든 사격이 빗나감이 된다 — blueprint 없는 배용 분기가 같은 커밋에 와야 한다.
- 동시 판정으로 바뀌면 §5의 「같은 사람의 같은 칸을 두 사람이 쏘면 둘 다 명중」이 클래식에도 적용되고, 현재 `:264`의 중복 사격 거부와 `:274`의 즉시 종료를 라운드 끝으로 옮겨야 한다.

---

## 14단계 — 종료 화면 (B-26)

`FleetGame.tsx:191-195`(관전자 시점 승패 미산출 — `viewerId`와 `winnerId` 비교가 코드 어디에도 없다) · `:219` · `:229` · `:208`(상대 보드에만 ' · 탈락', 내 보드는 항상 「~의 보드」).

- **마지막인 이유**: 이 항목이 만지는 네 자리가 전부 앞 단계의 산출물이다 — `:219`는 6단계 어휘, `:229`는 6·9단계, `:208`은 7·10단계, 그리고 승패 확정 시점 자체가 13단계에서 라운드 끝으로 옮겨진다. 먼저 하면 네 번 다시 고친다.

---

## 병렬 배치 요약

| 동시 실행 가능 | 근거 |
|---|---|
| 1단계 두 항목 | `App.tsx:624` vs `:607` — 서로 다른 구획 |
| 2단계(결정 대기) ∥ 3단계(문서) ∥ 4단계(코드) | 결정 왕복 지연을 코드·문서 작업으로 덮는다. 3단계는 코드 파일 0개 |
| 4단계 레인 A / B / C | `styles.css` / `fleet.ts` / `App.tsx` — 파일 자체가 다름 |
| 7단계(표기 어휘) ∥ 8단계(조준) ∥ 9단계(배치) | 7=`:29·:71-74·:128`, 8=`:93-110·:160·:175·:209·:221`, 9=`:112-138·:215-217·:229`. 단 `:128·:134`가 7·9에 걸치므로 **9단계의 FleetBoard 작업은 7단계 뒤에 시작**한다 |

| 절대 병렬 금지 (같은 줄) | 항목 |
|---|---|
| `FleetGame.tsx:199` | D-1 · B-15 · B-16 · B-14 · B-7(사례④) → 5단계로 직렬화 |
| `FleetGame.tsx:221` | B-3 · B-6 · B-33 · B-7 → 4·8단계로 직렬화 |
| `FleetGame.tsx:229` | B-16 · B-22 · B-14 · B-26 · B-27 → 6→9→14 순 |
| `FleetGame.tsx:204·211` | B-12 · B-13 → 10단계 한 티켓 |
| `FleetGame.tsx:137` | B-19 · B-25 · D-2.5(aria) → 9단계 말미에 통합 |
| `fleet.ts:303-344` | B-2 · B-3 → 4단계 레인 B 한 커밋 |
| `fleet.ts:362-399` | B-9 · B-31 · D-2 → 7 → 11 → 13 순 |
| `styles.css:95-105` | B-11 · B-27 → 같은 사람이 연달아 |
| `styles.css:129-133` | B-15 · B-24 · B-33 → 5 → 8 → 9 순 |

## 하나를 고치면 여럿이 없어지는 항목 (레버 순)

1. **D-2 결정 1건 → B-4·B-5·B-25 3건** (같은 표기 파이프라인, 한 커밋)
2. **D-2.5 어휘 맵 1벌 → B-14·B-16 2건 +** B-12 셋째 줄·B-26 문구의 입력
3. **D-1 구현 1건 → 후보 3장 겹침 결함이 별도 수정 없이 소멸** (슬라이스 자체가 사라짐)
4. **B-15의 `selected` 규칙 승격 1줄 → B-33 절반 + 특수 배 버튼·산탄 토글의 "안 보임"이 함께 소멸**
5. **B-6 1건 → B-32의 관통탄 절반 + B-7의 사례 ② 소멸**
6. **B-9 1건 → 두 증상(칸 단위 재생·연출 중 입력) + 13단계가 물려받을 연출/게이트**
7. **D-3(a) 1건 → B-30 + B-12의 「남의 이름」 절반**
8. **B-22 1건 → B-23의 예측 불가 증상 대부분**