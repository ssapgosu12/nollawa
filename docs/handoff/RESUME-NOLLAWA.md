# Nollawa — 다음 작업

STREAM: nollawa-build · v8 · 2026-08-14 (v7 = 폰 검수 12항 요청, 6회차 검수·5라운드 배포로 갱신)

▶ **RESUME HERE → 라이브(`https://nollawa.pages.dev`)를 폰에서 열어 아래 다섯을 눈으로 확인하고, 결과를 `docs/PLAN.md` §14에 새 표로 적는다.** [폰 확인은 사용자만 할 수 있다]
**DONE-WHEN**: 다섯을 보고 통과·실패를 §14에 기록한다. 실패가 나오면 그 행이 다음 티켓의 모집단이 된다. **푸시는 끝났다**(프런트 `3a2767a`, 라이브 번들에서 「카드 상단 오프셋」 확인). **릴레이는 이번 라운드에 바뀌지 않았으므로 배포가 필요 없다**(`git diff 09a492a..HEAD -- relay/`가 비어 있다).
**볼 것** — ①**덱 섞기**: 8장이 계단식으로 쌓여 모인 뒤 맨 위 카드가 좌우 번갈아 지나가고 10px 줄무늬로 사라지는지, 그리고 **덱이 카드 크기로 작게 보이는지**(화면을 뒤덮던 것을 고친 자리다). 시간이 마음에 안 들면 그 페이지의 **슬라이더 13개**로 조절해 보고 원하는 값을 알려 주면 기본값으로 굳힌다. ②**주사위**: 굴리는 도중 눈이 한쪽으로 몰리지 않고 흩어지는지. ③**오목 백돌 선택**: 백돌을 고르면 내 돌이 흰색이고 상대가 검정으로 먼저 두는지, 금수가 선수(검정)에게만 걸리는지. ④**원격 3인 팀 투표**: 한 명만 투표해 확정된 뒤에도 판이 계속 진행되는지(이것이 판을 영구 잠그던 W5 결함이었다). ⑤**로비**: 오목·육목·리버시 이름이 한국어로 나오는지.
**HOW→** ④는 한 대에서 신원 셋을 만들어 본다 — 탭 A를 접속시켜 두고 다른 탭에서 `indexedDB.deleteDatabase('nollawa-device')` 실행 후 새로고침. **브라우저 창이 최소화되면 타이머가 조여져 검수가 성립하지 않는다.**
**ANCHOR**: `3a2767a` (2026-08-14; tests 251 passed, build 성공, 푸시 완료) — 재개 전 `git log 3a2767a..HEAD --oneline`과 `git status --porcelain`으로 대조.

## STATE

하루에 **다섯 라운드**를 돌렸다. 배포 → 피드백 → 다음 라운드를 반복했고 지시 원문은
`docs/spec/DIRECTIVES.md` D-015~D-025에 쌓였다. 테스트 18 → **251개**. 6회차에는 검수 전담 에이전트가
라이브 12항을 전수 확인해 10항 통과·결함 3건을 냈고 그 셋을 5라운드에서 고쳤다.

- **게임 넷**(사목·오목·육목·리버시)이 격자 렌더러와 탐색 코어를 공유한다. 오목은 렌주 금수(삼삼·사사·장목)
  이며 스왑 오프닝은 폐기됐다. 금수와 돌 색은 **그 판의 선수(starter)** 기준이다 — 흑 = 선수, 백 = 후수.
- **판**: 기본 13×13(오목·육목 13/15/19 선택) · 두 단계 착수(누르고 「확인」) · 마지막 수 표시 ·
  한 줄 표시가 판을 밀지 않음.
- **시작**: 판마다 선공 결정 — 사람끼리는 동전, AI 대전은 흑돌/백돌 선택. 공통 경로이며 게임별로 갈리는 것은
  라벨 한 함수뿐이다(D-023).
- **연출**: 덱 섞기가 D-025 사양대로 재구현됐다(스폰 8장 → 2장 섞기 → 10px 인터레이스, 총 2,670ms,
  슬라이더 13개). 주사위는 실제 눈 배치로 스크램블한다.
- **로드맵 좌표**: `docs/PLAN.md` §8 — M0·M1 전부 완료. 다음 칸은 **M2(요트 다이스 + 범용 점수판)**.
  T0에 남은 것은 함대 격침·체스다.
- **IN-FLIGHT**: 없음. 협업 세션 0, worktree·감시자 정리 완료. 증거는 `collab13/tracks-20260814{,b,c,d}/`.
- **구조 예산**: `src` 비테스트 1,455/1,600 · CSS 246/500 · `relay/worker.js` 273/285 · 직접 의존성 4개.
- **다음 티켓 후보**: 대국 화면 안내문의 「● 1번 · ■ 2번」이 색·좌석 변경과 어긋난다(§14 5라운드 절 말미).
- **새 게임을 추가할 때의 규율**(D-023): 티켓 완료 조건에 「선공 결정이 그 게임에서도 동작한다」를 넣는다.

## DECIDED (재논의 금지)

- **다음 판은 전원 동의**(D-008) · **방 단계는 "나갔는가"로만 되돌린다**(D-010) · **AI 대전은 6명 전원이 한 팀**(D-013) · **방장의 「플레이 시작」이 곧 자기 준비**.
- **투표 확정은 둘뿐**이다 — 팀 전원이 같은 곳이면 즉시, 그 밖에는 `(미투표자)×4+7`초(미투표 0명이면 1초). 2026-08-14 4회차 검수에서 15.07·11.00·1.00초로 실측 확인됐다(D-012).
- **네 게임 모두 같은 투표 경로를 쓴다.** 게임별로 가르는 분기를 다시 만들지 않는다.
- `dist/`를 추적하지 않는다. 티켓은 사용자가 "발행해"라고 할 때만 발행한다(D-007, 작업 카드 최대 3장).
- **세 트랙 동시 진행은 worktree로 격리한다.** 같은 체크아웃에 여러 세션을 넣지 않는다.

## OPEN (필요할 때만)

- **연출의 던지기 방식** — `table-effects.md` §1은 3D가 어려우면 대체안(림버스식 회전·마리오파티식 스크롤)을 쓰되 무엇을 왜 골랐는지 남기라고 요구한다. 구현은 위에서 떨어져 한 번 튕기는 방식이고 근거가 적히지 않았다. V7을 보고 사양과 구현 중 어느 쪽을 맞출지 사용자가 정한다.
- **M2 착수 시점** — V4~V10 검수 뒤에 정한다.

## TRAPS (단정 전 확인)

- **브라우저 창이 최소화돼 있으면 투표 타이머 검수가 성립하지 않는다.** 확정을 계산하는 것은 대표 클라이언트(보통 방장)이므로 그 탭이 보여야 한다.
- **룰렛은 동점일 때만 돈다.** 최다 득표 열이 하나면 즉시 놓인다.
- **릴레이는 별도 배포다.** `npx wrangler deploy --config relay/wrangler.toml`이 필요하고 **그 명령은 사용자가 직접** 친다. 이번 판에서 `relay/worker.js`가 바뀌었다(268줄) — **푸시만으로는 릴레이가 갱신되지 않는다.**
- **배포 반영은 번들 해시가 아니라 내용 문자열로 확인한다.**
- **worker 결과 카드가 형식에서 자주 깨진다**(이번 판 4회: 파일명 `-result` 누락·`outcome` 누락·`raw:` 빈 값·HEAD 없음). director가 살아 있으면 스스로 correction을 내므로 **먼저 `ls`로 생존을 확인하고 기다린다.** supervisor가 대신 result를 쓰면 `foreign-result`로 거부된다.
- **`npm test`가 `vitest`를 못 찾는다.** `./node_modules/.bin/vitest run`으로 돌린다.

## CONTEXT (필독 — 해당 절만)

- `docs/INDEX.md` — **여기서 시작한다.** 질문에서 문서·절로 가는 라우팅 표.
- `docs/PLAN.md` §14 — 실기기 검수 이력과 **「검수 대기 목록」(V4~V10)**. 이번 재개점의 모집단.
- `docs/plan/room-lobby.md` · `docs/plan/table-effects.md` — 사양 전문과 「구현 상태」 표(이번 판으로 8행이 구현 완료로 넘어갔다).
- `docs/spec/DIRECTIVES.md` D-008~D-014 — 사용자 지시 원문. 계획과 어긋나면 이쪽이 이긴다.
- `collab13/COLLAB-ISSUES-LOG.md` — 협업 제어면에서 관측된 문제(감시자 되먹임, park 복구 규칙의 한계). 다음 협업 전에 한 번 읽는다.

---

**REALITY CHECK** (시작 직전 1회): `git log 3a2767a..HEAD --oneline` · `git status --porcelain` · `./node_modules/.bin/vitest run` · `python C:/Users/apple/.collab3/control.py ls --root C:/Localai/boardgames`

**디스커버리**: 이 파일은 `docs/INDEX.md`의 「인수인계」 표와 `docs/PLAN.md` §8 말미에서 가리켜진다. 스트림이 끝나면 같은 파일을 v9로 교체하고 다른 스트림 파일은 덮어쓰지 않는다.
