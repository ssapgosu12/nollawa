# Nollawa — 다음 작업

STREAM: nollawa-build · v3 · 2026-08-14 (v2 = M1-LOBBY 검수 요청, 완료되어 교체)

▶ **RESUME HERE → 한 팀에 3명 이상을 모아 "한 수 투표"를 열고, 새 확정 타이머와 화면 알림이 사양대로 도는지 검수한다.** [solo 실행 — 브라우저 자동화로 supervisor가 직접 할 수 있다]
**DONE-WHEN**: 세 가지를 눈으로 확인하고 결과를 `docs/PLAN.md` §14에 새 행으로 적는다 — ①마지막 투표 뒤 `(아직 투표 안 한 사람 수)×4+7`초가 지나면 확정된다(전원이 투표했으면 1초) ②남은 시간이 5초 이하면 화면 가장자리가 검게 점멸하고(1초 주기, 3초부터 250ms) 남은 초가 화면 맨 아래 숫자로 뜬다 ③확정 뒤 **룰렛 연출이 완전히 멈춘 다음에** 돌이 놓인다.
**HOW→** 한 대에서 여러 사람을 만드는 법: 탭 A를 먼저 접속시켜 두고, 탭 B에서 `indexedDB.deleteDatabase('nollawa-device')` 실행 후 새로고침하면 탭 B만 새 신원을 받는다(연결 시점에 키를 한 번 읽어 보관하므로 A는 안 깨진다). 이 방법으로 셋을 만들고 방장이 참가자 카드의 `(…)` → 슬롯 이동으로 **왼쪽 열에 2명 이상**을 모아야 투표가 열린다. 각 팀에 1명씩이면 즉시 확정이라 타이머가 돌지 않는다. 확장이 끊겨 있으면 사용자에게 연결을 요청한다(claude.ai/chrome, 같은 계정).
**ANCHOR**: `dddbd08` (2026-08-13; tests 111 passed) — 재개 전 `git log dddbd08..HEAD --oneline`과 `git status --porcelain`으로 대조.

## STATE

티켓 넷(`M1-LOBBY`·`M1-LOBBY-2`·`M1-LOBBY-3`·`M1-VOTE-1`)을 완주해 라이브에 배포했다. 테스트가 18 → 111개로 늘었다. 로비·팀·투표·좌석·퇴장 흐름·AI 대전이 모두 들어갔고, 남은 것은 위 검수와 다음 티켓 발행뿐이다.

- **로드맵 좌표**: `docs/PLAN.md` §8 — M0과 M1 로비 계열 완료 → 남은 것은 **M1 본편(오목·육목·리버시 + 공용 탐색 코어)**.
- **IN-FLIGHT**: 없음. 협업 세션 0, 대기 결정 0.
- **잔여물**: `collab13/cards/blocked/` 8장은 전부 해소된 이력이다. 정리 불필요.
- **git 미추적 산출물**: `collab13/`(협업 제어면 상태)와 **`dist/`(2026-08-13부터 추적 제외)**.

## DECIDED (재논의 금지)

- **다음 판은 전원 동의**다. 한 명만 눌러도 시작하던 예전 해법은 폐기됐다(`DIRECTIVES.md` D-008).
- **방 단계(대국 중↔로비)는 "나갔는가"로만 되돌린다.** 판이 끝나도 「다음 판」으로 이어가면 대국 상태다(D-010).
- **투표 확정은 둘뿐**이다 — 팀 전원이 같은 곳에 투표하면 즉시, 그 밖에는 위 타이머. 예전 조건(최초 1표 15초, 과반 5초)은 폐기됐다(D-012).
- **AI 대전은 6명 전원이 한 팀**이고 AI는 격자 칸을 차지하지 않는다(D-013).
- **방장은 「플레이 시작」이 곧 자기 준비**다. 준비 표시 분모에서 빠진다(`room-lobby.md` OPEN-1).
- `dist/`를 추적하지 않는다. 정본 호스트 Cloudflare Pages가 소스를 받아 자기가 빌드한다.
- 티켓은 사용자가 **"발행해"라고 말할 때만** 발행한다(D-007). 그 전에는 사양만 쌓는다.

## OPEN (필요할 때만)

- **다음 티켓 순서** — 사용자 미결. 후보 둘: ①**플레이어 상태 5종**(대기·준비·게임 목록 보는 중·게임 중·연결 끊김)과 **AI 강도 보통/높음**(1초/3초 예산)과 「AI 생각중...」 표시 ②**동전·주사위·덱 섞기 연출**과 그것을 보는 연출 테스트 페이지. 각각 `room-lobby.md`와 `table-effects.md`의 「구현 상태」 표에서 상태가 "다음 티켓"인 행이다.

## TRAPS (단정 전 확인)

- **릴레이는 별도 배포다.** 프런트만 자동 배포되고 릴레이는 `npx wrangler deploy --config relay/wrangler.toml`이 필요하며, **그 명령은 supervisor 권한 정책에 막히므로 사용자가 직접 실행**한다. 화면 구석 빌드 표시는 프런트 것이라 릴레이가 낡은 것을 가린다.
- **배포 반영은 번들 해시가 아니라 내용 문자열로 확인한다**(Cloudflare가 자기가 빌드한다).
- **같은 브라우저의 일반 탭 여러 개는 같은 사람이다.** 뒤에 연 탭이 앞의 탭을 밀어낸다.
- **Cloudflare가 방 생성 첫 요청에서 세 번에 한 번꼴로 내부 오류를 낸다.** 우리 코드가 아니고 재시도로 회피해 두었다 — 다시 보이면 플랫폼 쪽을 의심한다.
- **티켓이 끝나면 「구현 상태」 표의 그 행을 "구현 완료"로 바꾼다.** 빠뜨리면 다음 티켓이 이미 끝난 일을 모집단에 넣는다(실제로 두 번 겪었다).
- **`npm test`가 `vitest`를 못 찾는다.** `./node_modules/.bin/vitest run`으로 돌린다.

## CONTEXT (필독 — 해당 절만)

- `docs/INDEX.md` — **여기서 시작한다.** 질문에서 문서·절로 가는 라우팅 표.
- `docs/PLAN.md` §14 — 실기기 검수 이력 넷(F1~F4·G1~G3·H1~H4·P1~P6·R1~R2)과 그때그때의 결정.
- `docs/plan/room-lobby.md` — 로비·팀·투표·AI 대전·퇴장 사양 전문과 **「구현 상태」 표(= 티켓 모집단)**.
- `docs/plan/table-effects.md` — 동전·주사위·덱·비네트·카운트다운 사양.
- `docs/spec/DIRECTIVES.md` D-008~D-014 — 사용자 지시 원문. 계획과 어긋나면 이쪽이 이긴다.

---

**REALITY CHECK** (시작 직전 1회): `git log dddbd08..HEAD --oneline` · `git status --porcelain` · `./node_modules/.bin/vitest run` · `python C:/Users/apple/.collab3/control.py ls --root C:/Localai/boardgames`

**디스커버리**: 이 파일은 `docs/INDEX.md`의 「인수인계」 표와 `docs/PLAN.md` §8 말미에서 가리켜진다. 스트림이 끝나면 같은 파일을 v4로 교체하고 다른 스트림 파일은 덮어쓰지 않는다.
