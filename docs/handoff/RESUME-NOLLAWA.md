# Nollawa — 다음 작업

STREAM: nollawa-build · v10 · 2026-08-18 (v9 = M2 요트 발행 대기, 함대 격침 첫 웨이브가 끝나고 재설계로 방향이 바뀌어 교체)

▶ **RESUME HERE → 함대 격침을 개별 수정이 아니라 구조 재설계로 간다. 설계 문서를 먼저 쓴다.** [solo 착수 · 설계가 서면 사용자 승인]
**왜 방향이 바뀌었나**: 실기기 검수와 입력 전수조사로 결함 49건이 나왔고, 사용자가 「하나하나 고치면 끝이 안 보인다 · 모든 문제를 해결할 구조를 만들고 그걸 구현하자」고 지시했다(D-054). 규칙이 데이터가 아니라 분기문으로 표현된 것이 뿌리다 — 예: 「판 밖 중심을 허용하는 탄」을 한 줄에 열거해서 8종 중 3종만 적혀 있었고 그 누락이 결함 두 건이 됐다.
**DONE-WHEN**: 설계 문서가 서고, `docs/plan/fleet-strike.md` §7 수정 원장 **49행 전부**를 그 설계에 대고 **①구조적으로 불가능해짐 ②여전히 개별 수정 필요 ③설계가 답을 못 함** 셋 중 하나로 분류한 표가 있다. ③이 다섯 행을 넘으면 설계가 덜 된 것이므로 멈추고 다시 본다.
**HOW→** ①상태 모양·라운드 절차·네 도메인 계약(배치기 / 사격 해석기 / 피해 적용기 / 라운드 절차)을 설계 문서 하나에 쓴다 ②49행 대조표를 만든다 ③통과하면 이행 계획(무엇을 어떤 순서로 갈아끼우고 테스트 414개를 어떻게 유지하는가)을 짠다 ④그 다음에 티켓이다. **이 단계에서 `src/` 변경은 0이다.**
**예산·중단**: 새 정본 문서 **1개(400줄 이내)** · 새 고유명사 0(기존 이름을 쓴다) · 새 모듈 수는 이행 계획에서 정하되 **지금은 0이고 사용자 승인 없이 늘리지 않는다** · 설계 문서가 `fleet-strike.md`보다 길어지면 과설계로 보고 멈춘다.
**검증**: 설계 초안이 서면 **Codex(gpt-5.6-sol)에 적대 검증**을 붙인다 — 같은 계열이 설계하고 검증하면 맹점을 공유한다.
**ANCHOR**: `51a98ab` (2026-08-17; tests 414 passed, build 성공, 푸시 완료) — 이 재개점 커밋 1건이 그 뒤에 있는 것이 정상. 재개 전 `git log 51a98ab..HEAD --oneline`과 `git status --porcelain`으로 대조.

## STATE

함대 격침 첫 웨이브(티켓 5장)를 병합했다. 테스트 391 → **414**. 원장 49행 중 **12행이 「완료」**(사용자 실기기 검수 통과), 1행이 재현 완료, 1행은 도달 불가로 결함이 아님이 밝혀졌고, 나머지는 미착수다. 사양 쪽으로는 §7 수정 원장이 서고 사양이 침묵하던 여덟 자리가 메워졌다. **재설계는 이 원장을 합격 기준으로 삼는다** — 「이 구조가 49건을 구조적으로 못 나게 하는가」.

- **로드맵 좌표**: `docs/PLAN.md` §8 **M3**(은닉 정보 + 체스 → 최소 출시). 함대 격침이 M3의 본체이고 체스가 T0의 마지막 게임이다.
- **IN-FLIGHT**: 없음. 협업 세션 0(`python C:/Users/apple/.collab3/control.py ls --root C:/Localai/boardgames`).
- **잔여물**: worktree 등록은 전부 해제됐고 `C:/Localai/boardgames-shell`·`-frame` 등 디렉터리 몇 개가 파일 잠김으로 남아 있다(무해). psmux에 `user_direct_shell_1`·`_2`(검수용, 사용자 소유)가 있다 — 건드리지 않는다.
- **git 미추적 산출물**: `collab13/`(제어면 상태·협업 증거) · `dist/` · `artifacts/`. 셋 다 git으로 복원 불가.

## DECIDED (재논의 금지)

- **개별 수정 티켓을 더 발행하지 않는다.** 재설계 설계가 서고 사용자 승인을 받은 뒤에 티켓이다.
- **설계는 메인 세션이 쓰고 적대 검증만 Codex에 맡긴다**(사용자가 이 진행 방식을 승인했다).
- 티켓은 사용자가 "발행해"라고 할 때만 발행한다(D-007). 작업 카드 **동시 최대 7장**이나 상한이지 목표가 아니다(D-046).
- 사용자 결정 D-035~D-054는 전부 확정이다 — 특히 조명탄·격침 표기(D-050·D-052), 관통탄은 판 둘레 너비 2 패딩 안이면 전부 허용(D-053).
- 완료 12행은 **사용자 검수를 통과한 것**이다. 재설계가 그 동작을 깨면 회귀다.

## OPEN (필요할 때만)

- **배포 시점** — 완료 12행을 지금 낼지, 세로 폰 블로커(B-42: 배 목록이 화면 폭을 넘어 잘려서 9척 중 8척만 놓을 수 있다)와 갱신 배너 가림(B-45)을 고친 뒤 낼지. supervisor 권고는 「고친 뒤」였고 사용자 답은 아직 없다.
- **B-44의 사양 결정** — 압박 고폭탄에 「기본 사격을 먼저 해야 한다」는 선행 조건이 코드에는 있고 사양에는 없다. 없앨지 사양에 명시할지 정해야 한다.

## TRAPS (단정 전 확인)

- **원장 §7 머리말에 `impl:FS-…` 토큰의 실제 값을 쓰면 그 설명 줄까지 세어져 모든 카드의 분모가 하나씩 늘어난다.** 실제로 한 번 밟았다. 분모는 항상 `grep -c`로 재검산한다(구현 열 합 = 총행 = 49).
- **`SHOW-ME`(화면을 찍어 보이라는 칸)를 worker의 완료 조건으로 쓰면 안 된다.** worker 실행 환경에는 브라우저가 0개라, 그렇게 걸었더니 한 트랙이 일을 다 하고도 커밋을 못 했고 다른 트랙은 자동 검증 전부 통과하고 스스로 0점을 매겼다. 미관측은 **supervisor의 판정 문제**다(D-049 1항).
- **도는 director는 부팅 시점의 규칙을 갖고 있다.** `collab13/PREFLIGHT.md`를 고쳐도 이미 뜬 세션에는 반영되지 않는다 — 전제가 틀린 director는 내리고 `RESET:`을 적은 카드로 재발행한다.
- **파일을 `src/` 안으로 옮기면 타입 검사 대상이 된다.** `vitest`만 보고 `npm run build`를 건너뛰면 놓친다(한 번 밟아 빌드가 깨졌다).
- **psmux로 새 Claude 세션을 열면 부모의 `CLAUDE_CODE_CHILD_SESSION` 표시를 물려받아 대화 기록이 저장되지 않는다.** 지난 검수 세션이 그래서 사용자 지시 하나를 통째로 잃었다. 세션을 만들면 그 변수를 지우고 기동한다.
- **`P0-ORPHAN` 신호는 스폰과 기록 사이의 경쟁으로 나는 오탐일 수 있다.** `ls`에서 `live=true`면 살아 있는 것이다.
- **`npm test`가 `vitest`를 못 찾는다.** `./node_modules/.bin/vitest run`으로 돌린다.

## CONTEXT (필독 — 해당 절만)

- `docs/plan/fleet-strike.md` — 함대 격침 사양 정본. **§7 수정 원장 49행**(재설계의 합격 기준) · §3(사격 8종과 밸런스 확정치) · §4(특수 배 7종) · §4.8(피격 표기와 배 텍스처) · §5(동시 진행과 사양이 침묵하던 자리를 메운 문장들).
- `docs/inspection/` 7문서 — `fleet-inspection-20260815.md`(1차 검수 33건) · `fleet-input-audit-20260816.md`(페이즈×액션 입력 전수조사, 신규 8건) · `fleet-inspection-20260817.md`(2차 검수, 12항목 통과 + 신규 4건) · `rca-fleet-33-process-20260816.md`(왜 33건이 났는가) · `fleet-fix-order-20260816.md`·`fleet-ticket-plan-20260816.md`(개별 수정 계획 — **재설계로 대체됐으므로 역사로만 읽는다**) · `fleet-shell-reproduction-20260817.md`.
- `docs/spec/DIRECTIVES.md` D-035~D-054 — 사용자 지시 **원문**. 계획과 어긋나면 이쪽이 이긴다.
- `docs/PLAN.md` §8(로드맵 M0~M5) · §11(구조 예산).
- 협업을 시작하면 `psmux-collab` 스킬이 제어면 정본이고, 신호 감시는 `agent-monitor` 스킬대로 **메인 세션이** `<root>/collab13/monitor/signals/*.md`를 arm한다(지금은 협업 세션이 0이라 arm할 것이 없다).

---

**REALITY CHECK** (시작 직전 1회): `git log 51a98ab..HEAD --oneline` · `git status --porcelain` · `./node_modules/.bin/vitest run` · `git worktree list` · `python C:/Users/apple/.collab3/control.py ls --root C:/Localai/boardgames`

**디스커버리**: 이 파일은 `docs/INDEX.md`의 「인수인계」 표와 `docs/PLAN.md` §8 말미에서 가리켜진다. 스트림이 끝나면 같은 파일을 v11로 교체하고 다른 스트림 파일은 덮어쓰지 않는다.
