# Nollawa — 다음 작업

STREAM: nollawa-build · v7 · 2026-08-14 (v6 = 2라운드 검수 요청, 3·4라운드 배포 완료로 갱신)

▶ **RESUME HERE → 라이브(`https://nollawa.pages.dev`)를 폰에서 열어 아래 목록을 눈으로 확인하고, 결과를 `docs/PLAN.md` §14에 새 표로 적는다.** [사용자만 할 수 있다 — 폰·태블릿 확인]
**DONE-WHEN**: 아래 열두 항을 보고 통과·실패를 §14에 기록한다. 실패가 나오면 그 행이 다음 티켓의 모집단이 된다. **푸시·배포는 끝났다**(프런트 `09a492a` · 릴레이 `6115fd82`, 라이브 번들에서 「선공을 골라 주세요」 확인).
**볼 것 (이번에 올라간 것)** — ①AI 대전에서 선공 선택 화면(회색 반투명 + 흑돌/백돌)이 뜨고 **「다음 판」에서도 매번 뜨는지** ②사람끼리는 동전이 뜨는지 ③격자를 눌러 미리보기 → 「확인」이 손에 맞는지 ④판이 13×13이고 게임 설정에서 13/15/19가 바뀌는지 ⑤마지막 수 표시가 눈에 들어오고 룰렛·금수 표시와 구별되는지 ⑥「AI 생각중」이 떠도 판이 밀리지 않는지 ⑦오목에서 흑일 때 금수 자리가 표시되고 못 두는지 ⑧연출 테스트의 덱이 한 덩어리로 갈라지는지·주사위가 튕긴 뒤 눈이 보이는지
**볼 것 (아직 한 번도 못 본 것)** — ⑨참가자 상태 5종 ⑩AI 강도 높음이 체감상 더 오래 생각하는지 ⑪3명 이상 한 팀에서 오목·육목·리버시가 팀 투표를 거치는지 ⑫대국 중 새로고침에서 판이 유지되는지
**HOW→** ⑨⑪⑫는 한 대에서 신원을 여러 개 만들어 본다 — 탭 A를 접속시켜 두고 다른 탭에서 `indexedDB.deleteDatabase('nollawa-device')` 실행 후 새로고침. **브라우저 창이 최소화돼 있으면 타이머가 조여져 검수가 성립하지 않는다**(방장 탭이 화면에 보여야 한다).
**ANCHOR**: `09a492a` (2026-08-14; tests 229 passed, build 성공, 푸시·배포 완료) — 재개 전 `git log 09a492a..HEAD --oneline`과 `git status --porcelain`으로 대조.

## STATE

하루에 **네 라운드**를 돌렸다. 배포 → 사용자 피드백 → 다음 라운드를 네 번 반복했고 지시 원문은
`docs/spec/DIRECTIVES.md` D-015~D-023에 쌓였다. 테스트 18 → **229개**. **푸시·릴레이 배포까지 끝났고
라이브가 최신이다.**

- 게임 넷(사목·오목·육목·리버시)이 격자 렌더러와 탐색 코어를 공유한다. 오목은 렌주 금수(삼삼·사사·장목),
  스왑 오프닝은 폐기됐다.
- 판: 기본 13×13(오목·육목 13/15/19 선택) · 두 단계 착수(누르고 「확인」) · 마지막 수 표시 · 한 줄 표시가
  판을 밀지 않음.
- 시작: **판마다 선공 결정** — 사람끼리는 동전, AI 대전은 흑돌/백돌 선택. 방식 판단은 게임과 무관한 공통
  경로이고 게임별로 갈리는 것은 라벨 한 함수뿐이다(D-023).
- 로비: 참가자 상태 5종 · AI 강도 · 「AI 생각중...」 · 카드 간격. 연출: 동전·주사위·덱 섞기·연출 테스트 페이지.
- **로드맵 좌표**: `docs/PLAN.md` §8 — M0·M1 전부 완료. 다음 칸은 **M2(요트 다이스 + 범용 점수판)**.
  그 뒤 T0에 남은 것은 함대 격침·체스다.
- **IN-FLIGHT**: 없음. 협업 세션 0, worktree·감시자 정리 완료. 증거는 `collab13/tracks-20260814{,b,c}/`.
- **구조 예산**: `src` 비테스트 1,345/1,600 · CSS 181/500 · `relay/worker.js` 273/285 · 직접 의존성 4개.
- **새 게임을 추가할 때의 규율**(D-023): 티켓 완료 조건에 「선공 결정이 그 게임에서도 동작한다」를 넣는다.

## DECIDED (재논의 금지)

- **다음 판은 전원 동의**(D-008) · **방 단계는 "나갔는가"로만 되돌린다**(D-010) · **AI 대전은 6명 전원이 한 팀**(D-013) · **방장의 「플레이 시작」이 곧 자기 준비**.
- **투표 확정은 둘뿐**이다 — 팀 전원이 같은 곳이면 즉시, 그 밖에는 `(미투표자)×4+7`초(미투표 0명이면 1초). 2026-08-14 4회차 검수에서 15.07·11.00·1.00초로 실측 확인됐다(D-012).
- **네 게임 모두 같은 투표 경로를 쓴다.** 게임별로 가르는 분기를 다시 만들지 않는다.
- `dist/`를 추적하지 않는다. 티켓은 사용자가 "발행해"라고 할 때만 발행한다(D-007, 작업 카드 최대 3장).
- **세 트랙 동시 진행은 worktree로 격리한다.** 같은 체크아웃에 여러 세션을 넣지 않는다.

## OPEN (필요할 때만)

- **연출의 던지기 방식** — `table-effects.md` §1은 3D가 어려우면 대체안(림버스식 회전·마리오파티식 스크롤)을 쓰되 무엇을 왜 골랐는지 남기라고 요구한다. 구현은 위에서 떨어져 한 번 튕기는 방식이고 근거가 적히지 않았다. V7을 보고 사양과 구현 중 어느 쪽을 맞출지 사용자가 정한다.
- **M2 착수 시점** — V4~V10 검수 뒤에 정한다.

## TRAPS (단정 전 확인)

- **브라우저 창이 최소화돼 있으면 투표 타이머 검수가 성립하지 않는다.** 확정을 계산하는 것은 대표 클라이언트(보통 방장)이므로 그 탭이 보여야 한다.
- **룰렛은 동점일 때만 돈다.** 최다 득표 열이 하나면 즉시 놓인다.
- **릴레이는 별도 배포다.** `npx wrangler deploy --config relay/wrangler.toml`이 필요하고 **그 명령은 사용자가 직접** 친다. 이번 판에서 `relay/worker.js`가 바뀌었다(268줄) — **푸시만으로는 릴레이가 갱신되지 않는다.**
- **배포 반영은 번들 해시가 아니라 내용 문자열로 확인한다.**
- **worker 결과 카드가 형식에서 자주 깨진다**(이번 판 4회: 파일명 `-result` 누락·`outcome` 누락·`raw:` 빈 값·HEAD 없음). director가 살아 있으면 스스로 correction을 내므로 **먼저 `ls`로 생존을 확인하고 기다린다.** supervisor가 대신 result를 쓰면 `foreign-result`로 거부된다.
- **`npm test`가 `vitest`를 못 찾는다.** `./node_modules/.bin/vitest run`으로 돌린다.

## CONTEXT (필독 — 해당 절만)

- `docs/INDEX.md` — **여기서 시작한다.** 질문에서 문서·절로 가는 라우팅 표.
- `docs/PLAN.md` §14 — 실기기 검수 이력과 **「검수 대기 목록」(V4~V10)**. 이번 재개점의 모집단.
- `docs/plan/room-lobby.md` · `docs/plan/table-effects.md` — 사양 전문과 「구현 상태」 표(이번 판으로 8행이 구현 완료로 넘어갔다).
- `docs/spec/DIRECTIVES.md` D-008~D-014 — 사용자 지시 원문. 계획과 어긋나면 이쪽이 이긴다.
- `collab13/COLLAB-ISSUES-LOG.md` — 협업 제어면에서 관측된 문제(감시자 되먹임, park 복구 규칙의 한계). 다음 협업 전에 한 번 읽는다.

---

**REALITY CHECK** (시작 직전 1회): `git log 09a492a..HEAD --oneline` · `git status --porcelain` · `./node_modules/.bin/vitest run` · `python C:/Users/apple/.collab3/control.py ls --root C:/Localai/boardgames`

**디스커버리**: 이 파일은 `docs/INDEX.md`의 「인수인계」 표와 `docs/PLAN.md` §8 말미에서 가리켜진다. 스트림이 끝나면 같은 파일을 v8로 교체하고 다른 스트림 파일은 덮어쓰지 않는다.
