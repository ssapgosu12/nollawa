# 사용자 지시 원문 (DIRECTIVES)

**이 파일은 가공하지 않는다.** 사용자가 친 말을 날짜와 주제만 붙여 그대로 보관한다.
계획 문서는 여기서 도출되며, 도출이 원문과 어긋나면 **원문이 이긴다.**

왜 원문을 따로 보관하는가 — 가공본만 남으면 몇 세대 뒤에 "사용자가 승인했다"는 문장이
모델이 쓴 문장을 순환 인용하게 된다(승인 세탁). 원문이 남아 있어야 대조가 가능하다.

읽는 법: 필요한 지시 하나만 찾아 읽는다. 각 항목의 제목 줄에 날짜와 주제가 있다.

---

## D-001 · 2026-08-12 · 메인 화면과 방 로비 구조

> 방 흐름에 대해서: 메인화면에 이 기기에서 플레이, 방 참여, 방 생성 이렇게 3개로 하고 이 기기에서를 누르면 바로 게임 목록으로, 방 참여 혹은 방 생성을 누르면 방 로비로 이어지게 하자. 방 로비에서는 상단에 화면높이의 20%정도 영역에 현재 게임이 나오고, 게임 제목과 함께 플레이 인원수, 태그(예: 파티, 협동 등) 나오도록 하고, 게임 카드영역 오른쪽에서 (...) 버튼을 두어서 누르면 게임 설정 변경(텍사스 홀덤을 예시로 들면 시작 크레드, 최대 배팅가능 크레드, 올인 여부 변경)할수 있게 하자. 아래쪽 60%에 2*3 격자로 현재 플레이어와 방 생성한사람에게 왕관표시(방장 표시) 띄우자. 사람들 옆에 현재 준비상태가 나오게 하자. 방 코드는 최상단에 큼지막하게 나오게 하자. 제일 하단에는 플레이 시작 버튼이 있고, 현재 준비상태인 플레이어 명수 나오게 하자. (현재 방이 4명인데 3명 준비인경우: '3/4 준비됨') 플레이 시작은 방장이 누를수 있고, 2명일경우에는 2명 다, 3명일경우에는 2명만 준비해도 되게, 4명과 5명일경우 3명 준비, 6명일경우 4명 준비여야지 시작할수 있게 하자. 게임 시작 버튼의 왼쪽에는 게임 변경 버튼을 두자. 방장이 아닐경우에는 게임 시작버튼이 비활성화되고, 게임 변경 버튼에서는 게임 목록만 볼수 있고 정작 선택은 안되게 하자. 방장의 경우에 각 유저 카드 옆에 (...) 메뉴를 두어서 누르면 팝업으로 추방, 방장으로 승격등의 메뉴를 띄우자.

**도출된 계획**: `docs/plan/room-lobby.md`

## D-002 · 2026-08-12 · 계획 문서를 쪼갤 것

> 내가 말한거 정리해서 메뉴 지정 명령에 저장해. 이후 그걸 가공해서 메뉴 시스템의 세부 plan에 넣는거야. (당연히 각 게임/기능마다 plan 따로있어야해. plan 문서가 너무 길어지는거 막기 위함. 만약 plan 안에 절을 만들고 그걸로 충분히 구별 가능하다고 하면, plan index나 how to read등 파일 만들어서 agent들이 색인이 편하도록 해. 나중에 plan이 수백kb가 되면 일부만 골라읽는 능력이 중요해. plan을 여러 파일로 쪼개면 이럴일이 적어져.)

**도출된 계획**: `docs/INDEX.md`(색인과 읽는 법) + `docs/plan/` 아래 기능·게임별 분할

## D-003 · 2026-08-12 · 팀 대전과 투표 (D-001의 정원 초과 처리를 정정)

앞서 "정원 초과면 시작 불가"로 답했던 것을 사용자가 직접 정정한 것이다. 그 답은 폐기한다.

> 아니다. 6명방에서 2인용 사목두는경우에 정정. 1팀 2팀을 나누고 (2*3명 있는 로비에서 왼쪽열와 오른쪽열로 구분. 팀명은 방장이 지정 가능, 기본값으로는 (콜라, 사이다), (짜장, 짬뽕), (부먹, 찍먹), (인도어, 아웃도어) 등등 재치있는 대립되는 문자열로 랜덤 지정하자. 두 팀에서 번갈아가며 두게 하고, 각 팀의 순서에는 투표 형식으로 하자. 모든 표가 한곳으로 모이거나, 최초 1표가 투표되고 15초 경과되거나, 과반이 투표하고 5초 지나면 그 선택지로 정해지게 하자. 사목의 경우에 각 라인의 아래에 점 모양 아이콘으로 몇명이 투표했는지 보이게 하자.

**도출된 계획**: `docs/plan/room-lobby.md` §8 (팀과 투표)

## D-004 · 2026-08-12 · 기능 지시의 범위 (구조 예산 규율 정정)

supervisor가 "소스 상한 820줄을 넘으니 승인해 달라"고 물은 데 대한 사용자의 답이다.
이 지시는 앞으로 모든 티켓에 적용된다.

> 소스 상한은 feature가 늘면 당연히 늘어나야하는게 맞음. 승인 없이 늘리지 않는다? 근데 이번에는 내가 기능을 만들라고 지시했잖아. 소스 분량을 동결하라는 등의 반대지시가 없는한, 기능을 만들라고 하는것은 기능을 구체화, 개발계획 수립, 소스 상한 새로 예측 및 수정, 티켓 발행등의 모든 작업을 포함해서(그게 당연한 v13의 기능이니까) 지시한거잖아. 알아서 상한 올리고 해야지.

**도출된 계획**: `docs/PLAN.md` §11 구조 예산

## D-005 · 2026-08-12 · 투표 동점 시 룰렛 연출 (D-003 보강)

supervisor가 "동점이면 먼저 표가 들어온 쪽"으로 도출해 둔 것을 사용자가 정정한 것이다.

> 동점이면 랜덤 연출 하자. 룰렛 돌아가는거마냥 하이라이트 표시가 최대 세 선택지를 순환하면서 점멸로 나타나고, 점점 선택지간 체류시간이 늘어나더니 한 선택지에서 멈춰버리게.

**도출된 계획**: `docs/plan/room-lobby.md` §8.2

## D-006 · 2026-08-12 · 방장의 슬롯 이동 (D-001 §3 유저 메뉴 보강)

> 추가적으로 팀 조정을 위해, 방장이 플레이어를 n번 슬롯으로 이동시키는 기능도 넣자. 플레이어 카드 옆에 (...) 버튼에서 추방하기와 방장 위임등을 넣으라 했는데, 그 화면에서 플레이어를 n번으로 이동시키는 기능도 넣자. 빈 슬롯이면 바로 이동되고, 목적지에 다른 플레이어가 있으면 둘이 위치를 바꾸는 식으로.

**도출된 계획**: `docs/plan/room-lobby.md` §3

## D-007 · 2026-08-12 · 티켓은 만들면 발행한다

> 티켓 만들면, 발행해. 당연히 티켓은 발행하기위해 만든거고, supervisor가 길게 들고있어봤자 딱히 쓸모없어. 내가 발행해 라고 하면 발행해서 보내는거야. 그냥 기능 제안만 하면 이런식으로 멀티턴 지시를 통해 티켓에 내용을 쌓는거고.

**해석**: 사용자가 "발행해"라고 하면 supervisor는 카드를 만들어 **그 자리에서 `up`까지 한다.**
기능 제안만 오면 발행하지 않고 사양을 쌓아 둔다. 쪼갤 때 **작업 카드는 동시 최대 7장**이다(2026-08-15 D-046로 3장에서 상향).

## D-008 · 2026-08-13 · 다음 판 시작 조건 정정, 준비 버튼 위치, AI 대전, PC 로비 배치

첫 문단은 **D-003 이전에 확정했던 F1(한 명만 눌러도 즉시 새 판)을 사용자가 뒤집은 것**이다.
`docs/PLAN.md` §14의 F1 행은 구현 완료 기록으로 남기고, 시작 조건만 이 지시로 대체된다.

> 모든 플레이어가 다 다음판으로 넘어가도록 눌러야지 다음판 열리는게 타당해. 문서를 고쳐.
> 그리고 방장 외의 플레이어는 플레이 버튼 위치에 준비 버튼이 있어야해.
> 그리고 여러명이서 ai와 대전하는거와 4명이서 2대2로 2인용 게임하는거 등 테스트도 하게 구현시켜.
> 방장이 게임 설정에서 ai와 대전을 켜면, 팀 1 팀 2 상관없이 같이 ai와 대전하고, 선택지 관련해서는
> 내가 지시했던대로 진행해. 그리고 pc에서 로비 플레이어 카드 ui가 상당히 배치가 어색해보여.
> pc용 화면에서도 모바일과 유사하게 플레이어 카드가 보여야해. (...) 메뉴의 위치도 어색했고.

**도출된 계획**: `docs/plan/room-lobby.md` §2(카드 배치·`(…)` 위치) · §4(하단 버튼) · §9(AI 대전) ·
`docs/PLAN.md` §14(F1 정정)

## D-009 · 2026-08-13 · 룰렛 순서, AI 착수 지연, 로비 복귀와 퇴장

M1-LOBBY-2 배포 직후 실기기 검수에서 나온 지시다. 4·5·6은 방 단계(`phase`)가 대국에서
로비로 되돌아오지 않는 하나의 원인에서 나온다.

> 1. 랜덤연출이 완전히 끝난다음에 돌아 놔져야함. 지금은 돌이 놔짐과 동시에 랜덤연출이 시작됨.
> 2. ai 대전등에 ai가 너무 즉각적으로 놓음. 1초정도 딜레이 두면 좋음.
> 3. 로비로 돌아가면 자동으로 준비 취소되어야함.
> 4. 로비로 돌아가기/타이틀로 나가기가 있어야함. 일반 유저가 로비로 돌아가면 게임에서도 나가져야함.
> (유령 멤버가 게임참여하는거 막기위해)
> 5. 방장이 로비로 돌아가기하면 모든 멤버가 로비로 나가져야함.
> 6. 준비 취소 버튼이 잘 안눌림. (게임하다가 로비로 나간경우에.)

**도출된 계획**: `docs/plan/room-lobby.md` §10(대국에서 나가기) · §8.2(룰렛 순서) · §9(AI 착수 지연)

## D-010 · 2026-08-13 · 방 단계 규율 정정, 그리고 판 위 연출 3종

앞 문단은 D-009의 P6("판이 끝난 뒤 방 단계를 되돌린다")을 사용자가 정정한 것이다.

> 판이 끝난뒤라고 하면 한판 끝나고 이어서 새판 시작하는 경우에도 적용될꺼같으니, 판이 끝난 뒤보다는
> 다른 규율로 방 단계를 컨트롤하자.

**확정(2026-08-13 사용자 선택)**: **나가는 행위만 방 단계를 되돌린다.** 판이 끝나도 「다음 판」으로
이어가면 계속 대국 상태이고, 「로비로 돌아가기」를 눌렀을 때만 방이 로비로 돌아간다.

> 추가적으로, 이참에 화면에 동전 던지기와 주사위 굴리기, 덱 섞기 이벤트를 넣자.
> 동전 던지기 하면 n개의 동전이 화면 가온데에서 화면을 뚫고 나오는 방향으로 던져지고 (탑뷰로 보이게끔)
> 바닥에 떨어지자. 앞면은 흰색 바탕에 검정폰트로 H, 뒷면은 검은 바탕에 흰 폰트로 T라고 적어두자.
> 주사위도 같은 방식으로 던져지고, 주사위는 6면체로만 우선 하자. 흰 주사위에 검은 눈이야.
> 덱의 경우에는 반투명 회색 레이어를 화면 전체에 깔고, 시야 한 가온데에 카드 덱을 스폰한뒤에 덱이
> 섞이고, 다시 하나의 덱 모양으로 돌아온 뒤 인터레이스 방식으로 좌우로 갈라져서 사라지게하자.
> 덱이 사라지면 반투명 회색 레이어 끄고. 나중에 카드 쓰는 게임에서 해당 연출로 덱이 섞였음을 알려주는거야.

같은 날 보강 지시(연출 노출 방식을 물었을 때 함께 왔다):

> 게임 목록에 연출 테스트 페이지를 두자. 그리고 아까 말 덜했는데, 동전과 주사위는 3d로 조금씩은
> 튕기게 해야해. 한번쯤 가볍게 땅에 튕기게. 만약 3d나 유사 3d가 힘들다면, 림버스식 코인 던지기
> (코인이 빙글빙글 돌다가 핑! 하면서 멈춤)와 마리오파티식 주사위(주사위 눈이 csgo 상자 열듯이
> 내용물이 스크롤되다가 틱! 하고 한번에 멈추게)로 대체해도 돼.
> 덱 섞는거는 1개만 있으면 돼. 덱 섞기는 애니메이션일뿐이고, 밑에 자막으로 ㅇㅇ덱이 섞이고있습니다.
> 같이 띄우면 되니까. 주사위는 최대 6개, 동전은 최대 12개까지 동시에 던지는게 가능하게 해.
> 3d일경우 주사위끼리 서로 어느정도 부딛히기도 해야해. 동전이나 주사위는 던지기! 버튼이 있는거랑
> 자동 던져지는 버전 2개를 만들어야해.

**도출된 계획**: `docs/plan/room-lobby.md` §10(방 단계) · `docs/plan/table-effects.md`(연출 3종)

## D-011 · 2026-08-13 · 플레이어 상태 5종, AI 강도 2단계

> 추가적으로 각 플레이어 상태(준비, idle)에다가 게임 목록 보는중, 게임중 상태를 추가해.

> 하나 더 해. ai 강도를 보통과 높음으로. 보통은 지금에서 아주 살짝 약하게 만들고, 높음은 최대 3초를
> 쓰게 해. 이전의 ai 최소 대기시간 1초는 보통 난이도 기준이고, 높음은 3초를 주도록 해. 모든 게임
> 공통으로 ai가 두기전 대기시간에는 ai 생각중... 이라고 표기해야해.
> 목적은 오목이나 사목등 1대1게임을 사람 여러명이서 ai와 대국할때 어려움을 주게 하기 위해서야.

**목적이 명시된 지시다** — 여러 명이 투표로 상의하면 사람 쪽이 유리해지므로, AI가 그만큼 강해질
손잡이를 준다. `PLAN.md` §6.5가 예견한 "반복 심화 + 시간 예산" 손잡이가 이것이다.

**도출된 계획**: `docs/PLAN.md` §6.5(강도 2단계) · `docs/plan/room-lobby.md` §2(플레이어 상태) · §9(강도 선택)

## D-012 · 2026-08-13 · 투표 확정 타이머 교체, 비네트와 카운트다운 도구화

D-003 §8.2의 확정 조건 ②(최초 1표로부터 15초)와 ③(과반 투표 후 5초)을 **교체**한다.
①(팀 전원이 같은 곳에 투표하면 즉시)은 그대로 남는다(2026-08-13 사용자 선택).

> 투표제에 대해 업데이트: 마지막 투표가 이루어지고 n초 이후에 자동적으로 룰렛 돌아가게 해.
> n은 아직 투표 안한사람수×4+7초야. n이 0이면 1초로 고정하고 룰렛 돌려. (이때 후술할 비네트와
> 타이머는 꺼.)
> 5초 남았을때부터 화면 가장자리만 아주 가볍게 검정색 그래디언트(비네팅 비슷하게)로 1초마다 한번씩
> 점멸시켜. 3초부터 점멸주기 250ms로 줄여.
> 숫자도 화면 제일 하단에 나오게 해.
> 비네팅과 카운팅 기능도 다른 게임에서 쓸수있는 도구로써 존재해야해. 동전/주사위던지기나 투표후
> 룰렛도구마냥.

**해석 하나를 명시한다**: `n = 미투표자 × 4 + 7`이므로 n 자체는 0이 될 수 없다. "n이 0이면"은
**아직 투표하지 않은 사람이 0명일 때**를 가리키는 것으로 읽는다.

**도출된 계획**: `docs/plan/room-lobby.md` §8.2 · `docs/plan/table-effects.md` §6·§7

## D-013 · 2026-08-13 · AI 대전은 6명 전원이 한 팀

D-008의 AI 대전을 사용자가 정정한 것이다. 3회차 검수에서 격자가 여전히 좌우로 갈려
오른쪽 열이 AI 자리처럼 보이는 것을 사용자가 관측했다.

> 발행하는데, 추가적으로 ai대전 누르면 6명 전체가 한 팀이 되는거야. 지금처럼 3명 ai팀 3명 인간팀이면 안돼.

**도출된 계획**: `docs/plan/room-lobby.md` §9

## D-014 · 2026-08-13 · 게임 이름 옆에 (AI 대전) 표기

> 그리고 게임 명 옆에 사목 (AI 대전) 이라고 표기되어야해.

**도출된 계획**: `docs/plan/room-lobby.md` §2 — AI 대전이 켜진 동안에만 붙는 꼬리표다.

## D-015 · 2026-08-14 · 연출 검수 피드백 — 덱 섞기 재설계, 결과 무작위, 주사위 굴림감

> 연출 피드백:
> 1. 동전과 주사위 모두 3d가 아님.
> 2. 연출 시험 들어가자마자 덱이 섞임.(누른적 없는데도.)
> 3. 덱 섞이는게 과도하게 길음. 처음부터 덱이 갈라진채로 들어옴. 원하는것은 1개의 덱 나타남 -> 덱이 반반 갈라짐 -> 다시 합쳐짐 -> 인터레이스 방식으로 서서히 사라지면 되는데, 현재 상태는 1게이 덱이 1장씩 빠르게 내려오면서 나타남 -> 반반 갈라지고 섞임 -> 다시 덱이 합쳐지기도 전에 한번 더 섞임 -> 또 섞임 -> 또 섞임 -> 거의 페이드와 유사하게 반 갈라지면서 사라짐. 인터레이스로 사라지는것도 너무 줄 간격을 세밀하게 해서 연출이 페이드인것 같음. 한 10px단위로 묶어서 한줄로 처리해야 보기에도 깔끔한 인터레이스임.
> 4. 동전과 주사위 던지는거 결과가 랜덤이 아님. 동전은 H T 반복이고 주사위는 1~6을 순회함.
> 5. 동전은 던질때 2d 사양이니 안튕기는게 맞는데 (그리고 빙글빙글 돌면서 내려오다가 멈추는 그런 연출이기도 하니 결과가 충분히 랜덤인것처럼 연출되는데), 주사위의 경우에는 위에서 떨어지는 명백한 굴리는 모션이다보니 현행은 주사위를 굴릴떄 오는 심리적 기대감같은게 모션에 전혀 반영되지 않음. 한번쯤 튕기고 나서 내용물이 보였으면 좋겠음. 좀더 결과가 뒤죽박죽인것처럼 연출되면 좋겠음. (동전은 HT 교차인것으로 고정이지만 연출이 랜덤인것처럼 잘 보여서 실제 값만 HT 랜덤으로 하면 됨)

**도출된 계획**: `docs/plan/table-effects.md` §1·§2·§3·§4를 아래로 갱신한다.

- **덱 섞기는 부르는 쪽이 시작시킬 때만 재생한다.** 테스트 페이지를 여는 것만으로 재생되면 안 된다(2번).
- **덱 섞기 순서는 넷이다** — ①덱 하나가 나타난다 ②반반 갈라진다 ③다시 합쳐진다 ④인터레이스로 서서히 사라진다. 섞기 반복은 한 번뿐이고, 합쳐지기 전에 다시 갈라지지 않는다. 카드가 한 장씩 떨어져 덱이 만들어지는 도입은 쓰지 않는다(3번).
- **인터레이스 줄 두께는 약 10px 단위로 묶는다.** 줄이 가늘면 페이드로 보인다(3번).
- **동전과 주사위의 실제 결과는 무작위다.** 교차·순회 같은 고정 패턴을 쓰지 않는다(4번). 연출 부품 자체는 여전히 결과를 만들지 않고 부르는 쪽이 정한 값을 재생한다(§1 [도출] 유지) — 무작위를 만드는 것은 테스트 페이지처럼 부르는 쪽이다.
- **동전은 현행 유지**다. 2D 대체안(빙글빙글 돌다 멈춤)이고 튕기지 않는 것이 맞다(5번).
- **주사위는 굴림감을 넣는다** — 위에서 떨어져 **한 번쯤 튕긴 다음에 눈이 보인다.** 떨어지는 동안과 튕기는 동안에는 결과를 알 수 없어야 하고, 눈이 뒤죽박죽 바뀌다가 멈추는 것처럼 보여야 한다(5번).

## D-016 · 2026-08-14 · UI 간격과 AI 버튼, 선공 결정 연출, AI 첫 수, 오목 금수 표기

> 그 외 UI 피드백:
> 1. 이 기기 플레이에서 아무 게임이나 ai와 대전한뒤 로비로 돌아오면 ai와 대전하기 버튼이 사라져있음. (두사람이 시작 버튼만 남아있음)
> 2. 게임 목록에서 각 게임간의 간격이 너무 붙어있음.
>
> 게임 피드백:
> 1. 오목이나 육목 등 게임할때 ai가 선수라면 무조건 가운데에 두도록.
> 2. 애초에 ai가 선수로 두는걸 보지를 못함. 게임 시작할때 동전 던져서 선공 후공 정하는 연출 필요. 사람간에 대전할때도 마찬가지로 선공 연출 필요.
> 3. 오목에서 렌주룰에 따른 33 금수 등 표기 필요.

**도출된 계획**

- **UI-1**: 결함이다. AI 대전을 한 판 하고 게임 목록으로 돌아오면 화면 모드가 `ai`로 남아 「AI와 시작」의
  표시 조건이 깨진다. 목록으로 돌아올 때 모드를 되돌린다. (`room-lobby.md` §6)
- **UI-2**: 게임 목록 카드 사이 간격을 넓힌다. (`room-lobby.md` §6)
- **게임-1**: 오목·육목처럼 빈 판에서 시작하는 게임은 **AI가 선수면 첫 수를 판 한가운데에 둔다.**
  탐색 결과와 무관하게 고정한다. (`room-lobby.md` §11)
- **게임-2**: **판을 시작할 때 동전을 던져 선공·후공을 정하고 그 과정을 보여준다.** AI 대전과 사람끼리
  대전 모두에 적용한다. 동전 연출(`table-effects.md` §2)을 부르는 첫 실사용처다. (`room-lobby.md` §11)
- **게임-3**: 오목에 **렌주룰 금수(삼삼 등) 표기**를 넣는다. (`room-lobby.md` §11 · **사용자 결정 필요**)

**사용자 결정이 필요한 갈래 (게임-3)**: 계획서 §T0는 오목을 「자유룰 + 스왑 오프닝」으로 잡았고,
조사 문서(`docs/research/catalog/S10-구현난이도 매트릭스.md`)는 **금수 대신 스왑을 쓰면 10줄로 끝나고
금수를 넣으면 난이도가 급등한다**는 이유로 금수를 백로그에 두라고 결론냈다. 스왑과 금수는 둘 다 흑의
선수 이점을 없애는 장치라 보통 **둘 중 하나만** 쓴다. 그래서 세 갈래가 있다 — ①금수를 넣고 스왑을
없앤다(렌주 정식에 가깝다) ②스왑을 유지한 채 금수는 **표시만** 하고 금지는 하지 않는다(경고 표기)
③둘 다 적용한다. 원문은 "표기 필요"이므로 ②로 읽히지만, ①·③과 결과가 다르므로 사용자가 고른다.

## D-015-a · 2026-08-14 · 덱 섞기 템포 정정 (D-015 3번 수정)

> 연출에 있어 추가 피드백:
> 1. 섞이는 과정에서 내가 두번 세번 섞는다 했는데, 그게 연속되게 빠르게 나온다면 한번 섞는것처럼 보일꺼같고, 오히러 딱 한번만 갈라졌다 합쳐지는것보다 매력적일꺼같음. 섞이고 쉬고 섞이고 쉬고 하는 대신에 바로 섞이고 섞이고 템포를 빠르게 섞는방향으로 내 지시사항 수정할것.

**도출된 계획**: D-015의 「섞기는 한 번뿐」을 **취소**한다. 갈라졌다 합쳐지는 동작을 **여러 번 연속으로,
쉬는 구간 없이 빠른 템포로** 잇는다 — 한 덩어리의 섞기로 보이는 것이 목표다. 나머지 순서(①덱 하나가
나타남 → ②③빠른 섞기 → ④인터레이스로 사라짐)와 10px 인터레이스, 자동 재생 금지는 그대로다.

## D-017 · 2026-08-14 · 오목은 금수로 간다 — 스왑 폐기

> 스왕 이라고 했는데, 스왕이 뭐지? 스왑이라는 뜻인가? 스왑은 너무 공식룰같으니까 그냥 선공 금수 금지만 하고 말자. 2번 옵션에 가까워.

**도출된 계획**: D-016 게임-3의 갈래 중 **①(금수를 넣고 스왑을 없앤다)** 를 채택한다.

- **스왑 오프닝을 폐기한다.** 지금 구현된 「돌 바꾸기」 버튼과 그 규칙(`src/game/omok.ts`의 swap 액션·
  `swapAvailable`)을 제거한다. 계획서 §T0의 「자유룰 + 스왑 오프닝」 표기도 함께 고친다.
- **선공(흑)에게 렌주 금수를 적용한다** — 삼삼·사사·장목(6목 이상)을 **금지**한다. 후공(백)에는 적용하지
  않는다. 금수 자리는 화면에 **표시**하고 그 자리에는 둘 수 없다.
- AI도 금수를 피해 두어야 한다. 금수 판정이 승패에 직결되므로 자동 테스트로 각 금수 유형을 덮는다.

## D-018 · 2026-08-14 · 바둑판형 게임은 13×13 기본, 두 단계 착수, 바둑은 크기 선택

> 추가적으로 할것
> 오목 등 바둑판 쓰는게임은 13×13을 기본으로.
> 또한 격자에 바로 두는것보다 놓고 확인 눌러야지 돌이 둬지도록 하자.
> 진짜 바둑을 추가할때는 1313, 1919로 선택 가능하게 하자.

**도출된 계획**

- **판 크기**: 교점에 두는 게임의 기본 판을 **13×13**으로 내린다. 지금은 오목 15×15, 육목 19×19다.
  폰 세로에서 한 눈이 24px 아래로 내려가면 손가락으로 집기 어려운데(PLAN.md §5 돌 렌더링 표), 13×13이면
  폰 390px에서 한 칸이 약 28px로 올라온다. 리버시 8×8은 칸에 두는 게임이라 해당 없다.
- **두 단계 착수**: 격자를 누르면 **미리보기 돌**이 놓이고, **「확인」을 눌러야 확정**된다. 잘못 짚어 그대로
  두어지는 것을 막는 장치다. 미리보기 상태에서 다른 자리를 누르면 미리보기가 옮겨간다.
- [도출] **원격 팀 투표와의 관계** — 팀 투표에서는 「확인」이 곧 **표 제출**이다. 표를 낸 뒤에도 다른 자리를
  누르면 미리보기가 옮겨가고 다시 확인하면 표가 바뀐다(표를 바꾸면 타이머가 다시 시작하는 기존 규칙 그대로).
  혼자 두는 판(공용·AI)에서는 「확인」이 곧 착수다.
- **바둑(추가 시점에)**: 판 크기를 **13×13과 19×19 중 고를 수 있게** 한다. 바둑 자체는 아직 T1 백로그이며
  이 항목은 그때 함께 만든다.

## D-019 · 2026-08-14 · 오목·육목도 판 크기 선택 (D-018 증보)

> 오목 육목도 바둑마냥  1313 1515 1919 선택 가능하게 하자.

**도출된 계획**: D-018의 「13×13 기본」은 유지하되 **고정이 아니라 기본값**이다.

- 오목과 육목은 **13×13 · 15×15 · 19×19** 중에서 고를 수 있고 **기본은 13×13**이다.
- 바둑(T1 백로그)은 D-018대로 13×13 · 19×19 둘 중에서 고른다.
- [도출] 고르는 자리는 **방 로비의 게임 설정 팝업**이다 — AI 강도와 같은 자리이고 방장만 바꾼다.
  이 기기에서 플레이(공용)에서는 게임을 시작하기 전 같은 값을 고를 수 있어야 한다.
- [도출] 판 크기는 **판이 시작되기 전에만** 바꿀 수 있다. 대국 중 변경은 판을 버리는 것과 같다.
- [도출] 승리 조건은 판 크기와 무관하다 — 오목은 5목, 육목은 6목 그대로다. 렌주 금수(D-017)도
  세 크기 모두에 같게 적용한다.

## D-020 · 2026-08-14 · 덱이 쪼개져 보인다, 주사위 눈이 너무 빨리 바뀐다

> 연출 피드백:
> 1. 덱이 사진처럼 쪼개져버림.
> 2. 주사위가 튕기면서 두세번만 바뀌면 되는데 너무 빠르게 많이 변함.

(1번의 사진: 폰 세로 화면. 덱이 사선 줄무늬가 굵게 들어간 **두 조각으로 나뉘어 서로 기울어진 채 벌어져**
있고, 한 덩어리의 덱이 아니라 부서진 카드 두 장처럼 보인다.)

**도출된 계획**

- **덱은 갈라져도 한 덩어리로 읽혀야 한다.** 반으로 나뉜 두 뭉치가 **기울어지거나 멀리 벌어지지 않게** 한다 —
  섞는 동안에도 나란히 붙어 있고, 사람이 카드를 반으로 떼어 맞물리는 그림이어야 한다. 지금은 벌어짐과
  기울기가 커서 부서진 것처럼 보인다.
- **카드 뒷면의 사선 줄무늬가 굵고 강하다.** 카드 한 장으로 읽히도록 무늬를 약하게 하거나 뒷면답게 바꾼다.
  인터레이스(10px 가로 줄)는 **사라질 때만** 쓰는 장치이고 카드 면의 상시 무늬가 아니다.
- **주사위 눈은 튕기는 동안 두세 번만 바뀐다.** 지금은 너무 빠르고 많이 바뀌어 깜빡임처럼 보인다.

## D-021 · 2026-08-14 · 「AI 생각중」이 판을 밀어냄, 마지막 수 표시 필요

> 1. ai 생각중일때 보드 레이아웃 전체가 아래로 밀림. (사진 참조)
> 2. 마지막으로 상대방이 둔곳이 어디인지 표기가 필요함.
> 3. 아직도 보드에 직접 두는 방식임. 보드 사이즈도 아직 조정 안됨. 정상?

(사진: 폰 세로. 「1번 차례」 화면과 「2번 차례 / AI 생각중...」 화면을 나란히 두면 판이 한 줄 높이만큼
아래로 내려가 있다. 3번에 대한 답 — **정상이다.** 두 단계 착수(D-018)와 판 크기(D-018·D-019)는 사양만
쌓아 둔 상태이고 티켓을 발행하지 않았으므로 라이브에 없다. 오목은 아직 15×15다.)

**도출된 계획**

- **「AI 생각중...」이 레이아웃을 밀지 않는다.** 그 줄의 자리를 항상 잡아 두거나(빈 자리 예약) 판 위에
  겹쳐 띄운다. 표시가 켜지고 꺼질 때 판의 위치가 움직이면 안 된다. 같은 자리를 쓰는 다른 표시
  (「상대가 새 판을 시작했습니다」·좌석 표시·카운트다운)도 같은 규율을 따른다.
- **마지막 수를 표시한다.** 가장 최근에 놓인 돌이 어디인지 판에서 알 수 있어야 한다. 상대가 둔 자리를
  놓치지 않는 것이 목적이므로 **모든 게임 공통**으로 넣고, 그 표시는 룰렛 하이라이트나 금수 표시와
  구별돼야 한다. 여러 개가 한 번에 놓이는 육목에서는 그 턴에 놓인 것 전부를 표시한다.

## D-022 · 2026-08-14 · 선공 결정은 매 판, AI 대전은 동전이 아니라 사용자가 고른다

> 오목 ai대전할때 첫반의 선수는 동전던지기하지만 이후에는 동전 안던짐.
> 그리고 ai대전에서는 동전이 아니라 사용자가 고를수있었으면 좋겠어.
> 똑같이 회색 반투명으로 화면 전체 덮고, 흑돌(선수) 백돌(후수) 버튼으로.

**도출된 계획**

- **선공 결정은 판이 시작될 때마다 한다.** 지금은 첫 판만 하고 「다음 판」으로 이어지는 판에서는 건너뛴다.
  이것은 결함이며 D-016의 "판을 시작할 때"는 첫 판만을 뜻하지 않는다.
- **AI 대전에서는 동전을 던지지 않고 사람이 고른다.** 화면 전체를 **회색 반투명 레이어**로 덮고(덱 섞기와
  같은 방식) **「흑돌 (선수)」·「백돌 (후수)」 두 버튼**을 띄운다. 고르면 그 자리에서 판이 시작된다.
- **사람끼리 대전은 동전을 그대로 던진다.** 고르게 하면 서로 합의할 방법이 없다.
- [도출] 버튼의 돌 표기는 게임의 표현을 따른다 — 오목·육목은 흑돌·백돌, 사목과 리버시는 그 게임이 쓰는
  1번·2번 표기를 쓴다. 어느 쪽이든 **선수·후수를 괄호로 함께 적는다.**
- [도출] 공용(한 기기에서 두 사람이 번갈아) 플레이는 사람끼리 대전이므로 동전을 던진다.

## D-023 · 2026-08-14 · 선공 결정은 모든 게임 공통 절차다

> 앞으로 다른 게임 개발할때 (배틀쉽 등) 자동적으로 선공 후공 정하는게 있을꺼지?
> 선공후공 정하라는말은 오목이나 육목에만 해당하는 이야기가 아니라 모든 게임 공통이여야하는거야.

**도출된 계획**

- **선공 결정은 게임별 기능이 아니라 판 시작 공통 절차다.** 사목·오목·육목·리버시는 물론 앞으로 추가하는
  게임(함대 격침·체스·요트 다이스·벽길 …)에도 **새로 만들지 않아도 붙어 있어야 한다.**
- **구현 규율**: 공통층(판 시작 흐름)에서 한 번 처리하고 게임별 코드에 복제하지 않는다. 게임을 카탈로그에
  등록하면 선공 결정이 자동으로 따라오는 구조여야 하며, 게임마다 선공 결정 코드를 쓰는 형태는 실패다.
- [도출] **차례가 있는 게임 전부**에 적용한다. 차례 개념이 없는 것(범용 점수판처럼 순서 없이 점수만 적는
  도구)에는 해당하지 않는다. 동시 진행 게임(빙판처럼 모두가 동시에 지시하는 것)은 선공 개념이 없으면
  건너뛰고, 순서가 필요하면 같은 절차를 쓴다.
- [도출] 표기만 게임을 따른다 — 흑돌·백돌이 없는 게임은 그 게임의 좌석 표기(1번·2번)를 쓰고 선수·후수를
  괄호로 병기한다(D-022). 사람끼리 대전은 동전, AI 대전은 사람이 고르는 것도 게임과 무관하게 같다.
- [도출] 새 게임 티켓의 완료 조건에 **"선공 결정이 그 게임에서도 동작한다"** 를 넣는다. 게임을 추가하면서
  이 절차를 우회하면 그 티켓은 미완이다.

## D-024 · 2026-08-14 · 덱 반갈죽 3회째, 주사위 눈이 한쪽으로 쏠림

> 1. 덱 섞기가 점점 퇴화중. 아직도 덱 반갈죽 남.
> 2. 주사위 굴리는도중에 눈이 전부 한쪽으로 쏠림.

(사진: 폰 세로 다크모드. 덱은 흰 테두리 안 크림색 카드 한 장처럼 보이는데 **정중앙에 세로 이음선**이
그어져 있다. 주사위는 굴러가는 도중 눈 세 개가 **한 열에 세로로 몰려** 있다.)

**도출된 계획**

- **1번은 세 번째 같은 증상 보고다.** D-015(과도한 길이·페이드) → D-020(쪼개져 보임) → 이번이 3회째다.
  `two-strikes` 규율에 따라 세 번째 변형 수정을 만들지 않고 원인 귀속부터 다시 했다. 원장과 재검토는
  `docs/handoff/DEBUG-DECK-SHUFFLE.md`에 있다. 요지 — 기각된 두 가설은 「얼마나 움직이는가」를 다뤘고,
  생성 지점은 **덱을 처음부터 두 조각(`shuffle-left`·`shuffle-right`)으로 만드는 구조**다. 합쳐진 상태조차
  두 반쪽을 겹쳐 흉내내므로 경계가 남는다. 수리는 값 조정이 아니라 **한 장의 덱을 실제로 두고 갈라지는
  구간에서만 반쪽을 보이게** 하는 구조 변경이다. 구현 전에 확인할 관찰 하나가 남아 있다(모션 줄이기에서
  정지된 합쳐진 상태를 캡처).
- **2번(주사위 눈 쏠림)의 원인은 코드에서 바로 보인다.** 눈 9개는 3×3 격자에 행 우선으로 놓이는데,
  스크램블 변형이 `nth-child(3n+2)`·`nth-child(3n)`으로 갈린다 — 그 나머지 그룹이 **열**과 일치하므로
  한 순간에 한 열 전체가 켜지고 눈이 한쪽으로 몰려 보인다. 스크램블 단계에서는 **실제 주사위 눈 배치처럼
  흩어져 보여야** 하며, 열 단위로 묶여서는 안 된다.

## D-025 · 2026-08-14 · 덱 섞기 애니메이션 사양 (사용자 구현안)

> 내가 생각하는 구현안
> 1. 카드 8장정도로 구성덴 덱을 촤라락 하고 위에서 살짝 떨구듯이 하여 덱을 스폰시킨다. 8장 각각 약간의 딜레이를 두고 페이드인.
> 카드는 내쪽에서 멀어지는 방향으로(화면 구성상 이전 카드보다 화면 상단쪽으로) 한장씩 계단식으로 쌓인다.
> 1장당 300ms이며 카드간 간격은 70ms이다. 즉 첫번째 카드 떨어지는 모션이 230ms남았을때 두번째 카드가 스폰한다. 페이드인은 위에서 떨어지는 첫 150ms에 완료하도록 한다. 떨어지는 속도는 초기속도+초당 가속으로 자유낙하 비슷하게 보이게 한다. 멈출때는 목적지에 거의 다다랐을때 기계적인 감속처럼 보이게 한다.(마치 유압 댐퍼처럼) 혹은 초기 속도가 있는 spring 곡선 사용. 카드 8장 다 스폰 애니메이션이 끝났다면 300ms 대기 후 카드는 아래에서 4번째 카드 위치를 기준으로 모인다. 마치 1장의 카드만 있는것처럼 보이게.
> 2. 덱을 8장대신 2장의 카드로 바꾼다. 어차피 덱을 1장처럼  뭉쳐놨기에 비주얼상 차이는 없다. 이후 2장의 카드를 한장은 왼쪽, 한장은 오른쪽으로 카드 너비의 30%씩 이동시킨다. 왼쪽카드는 반시계방향으로 20도, 오른쪽 카드는 시계방향으로 20도 기울인다. 이동과 기울임은 동시에 일어나야한다.(이걸 덱 분리, 이거의 역을 덱 합치기라고 하겠다.) 애니메이션 지속시간은 150ms. 곡선은 spring.
> 이후 100ms 대기 후 합쳐지게 spring, 100ms. 이후 70ms 대기 후 100ms동안 분리, 70ms 대기 후 100ms동안 합쳐지기를 2번 반복.
> 합쳐지고 나서는 인터레이스 방식으로 좌우로 갈라지며 사라지게. 페이드아웃, 움직임은 리니어.
>
> 핵심은 제일 위에있는 카드가 중앙-오른쪽-중앙-왼쪽으로 좌우 번갈아가며 지나가야한다는점이다. 모든 ms는 일단 슬라이더로 만들어서 조정가능하게 하자.
> 곡선류는 내가 언급한걸로 고정.
> 그리고 화면 최하단에 있는 덱 섞이는중 메세지는 덱의 바로 아래, 카드너비의 20%정도 띄워진 위치에 표기하자.

**도출된 계획**: `docs/plan/table-effects.md` §4를 이 사양으로 교체한다. 세부는 그 문서의 표를 정본으로 삼는다.
이 지시는 D-015·D-015-a·D-020·D-024가 다루던 "덱이 반갈죽으로 보인다"의 **원점 재검토 결과를 대체하는
사용자 구현안**이다 — 즉 구조를 사용자가 직접 정했으므로 supervisor의 가설 #3(한 장을 두고 갈라질 때만
반쪽을 보이기)은 폐기하고 이 사양대로 만든다. 스폰 구간은 8장, 섞기 구간은 2장으로 **단계마다 카드 수가
다르다**는 것이 이 안의 핵심이며, 그래서 합쳐진 상태의 이음선 문제가 원리적으로 사라진다.

## D-026 · 2026-08-14 · 덱 섞기가 섞지 않는다, 모이기는 홀짝 분리로 대체

> 덱 섞기에 대한 피드백: 1. 덱이 다 쌓인다음에 모이는 텀이 너무 길다. 2. 모인 다음에 분리-합치기등을 거치지 않고 잠시 있다가 그냥 인터레이스 페이드 아웃 되어버린다.
> 2번은 고쳐야하는데, 1번의 경우에는 그냥 안모으고 홀수번째,짝수번쨰 카드로 나누어서 섞는것 괜찮을듯?

**도출된 계획**

- **2번은 결함이다.** D-025가 정한 섞기 구간(분리·합치기 3회, 맨 위 카드가 좌우 번갈아)이 **실제로 재생되지
  않고** 곧바로 인터레이스 소멸로 넘어간다. 사양이 아니라 구현이 틀린 것이다. supervisor가 병합 전에
  구조(카드 수·clip-path 없음)와 CSS 키프레임만 확인하고 **실제 시퀀스를 프레임으로 보지 않아** 통과시켰다.
- **1번은 사양 변경이다.** 스폰 뒤 「모이기」 단계를 **없앤다.** 대신 쌓인 8장을 **홀수번째와 짝수번째 두 뭉치로
  나누어** 그 두 뭉치로 분리·합치기를 한다. 8장을 2장으로 교체하던 D-025 §4.2의 방식을 이것으로 대체한다.
- [도출] 홀짝 분리의 이점 — 카드가 이미 계단식으로 쌓여 있으므로 모으는 시간이 필요 없고, 실제로 카드를
  반으로 떼어 맞물리는 그림에 더 가깝다. 좌우로 나뉘는 각 뭉치는 4장이며, 이동·기울임 값(카드 너비 30%,
  좌우 20°)과 시간표(분리·합치기 3회, 맨 위 카드가 좌우 번갈아)는 D-025 그대로다.
- [도출] 「모이기 ms」 슬라이더는 의미가 없어지므로 없앤다. 나머지 슬라이더는 유지한다.

## D-027 · 2026-08-14 · 슬라이더 값 확정, 카드 8장이 보이게, 카드 뒷면 텍스처

> 현재 내가 조절해둔 슬라이더 읽고, 10의 단위로 반올림한다음에 그값으로 적용시켜. 그리고 카드가 8장이여야하는데 비주얼로는 4장밖에 안나와. 그리고 카드가 기존 카드의 위에 쌓여야하는데, 카드 테두리 잔상때문에 방해돼. 카드의 뒷면에 간단한 텍스쳐를 넣으면 해결될꺼같아. 테두리는 지금처럼 유지하고. 예시 이미지 첨부했어.

(첨부 이미지: 손그림 카드 두 장이 비스듬히 겹쳐 있고 아래로 여러 장의 두꺼운 검은 테두리가 층층이 보인다.
카드 면에는 **사선 방향의 굵은 해칭선** 몇 줄이 그려져 있고, 가운데에 손글씨 **「Nollawa」** 가 적힌 리본형
배너가 있다. 흑백뿐이다.)

**도출된 계획**

- **슬라이더 실측값을 10 단위로 반올림해 기본값으로 굳힌다** — 카드 낙하 300 · 카드 간격 70 ·
  페이드인 100 · 스폰 후 대기 90 · 첫 분리 50 · 첫 분리 대기 0 · 반복 분리 50 · 반복 대기 0 ·
  합치기 300 · 소멸 500 (단위 ms). **낙하 시작 높이 160%**.
- [도출] **「카드 상단 오프셋」은 2%를 유지한다.** 10 단위로 반올림하면 0%가 되어 계단 쌓임이 사라지고,
  같은 지시의 「카드가 기존 카드 위에 쌓여야 한다」와 어긋난다. ms 항목만 반올림한다.
- **카드가 8장으로 보여야 한다.** 현재 DOM에는 8장(스폰 8 · 뭉치 2×4)이 있는데 화면에서는 4장으로만
  읽힌다 — 겹침 간격과 테두리 잔상 탓이다.
- **카드 뒷면에 간단한 텍스처를 넣는다** — 첨부 그림처럼 사선 해칭과 가운데 「Nollawa」 배너. **테두리는
  지금처럼 유지**한다. 텍스처가 있으면 겹친 카드의 경계가 테두리 잔상이 아니라 면으로 구분된다.
- [도출] 흑백만 쓰고 색은 CSS 변수로 참조한다(§1). 텍스처는 이미지 파일이 아니라 CSS 그라디언트나
  인라인 SVG로 만든다 — 직접 의존성과 네트워크 요청을 늘리지 않는다.

## D-028 · 2026-08-14 · 덱은 8장 쌓임만 남았고, 주사위 눈은 더 천천히

> 카드는 8장이여야하는데 쌓인건 4장뿐인 그 문제만 제외하면 괜찮아. 그 외로는 주사위굴리기할때 안의 숫자가 좀더 천천히 바뀌었으면 좋겠어.

**도출된 계획**

- **덱 섞기는 위 슬라이더 값(D-027)으로 사용자 확인을 받았다.** 남은 결함은 **쌓인 카드가 8장이 아니라
  4장으로 보이는 것** 하나뿐이다. 나머지(스폰·분리·합치기·소멸·자막·크기)는 이대로 유지한다.
- [도출] 원인 후보 — 홀·짝 두 뭉치가 각각 4장이고 같은 자리에 겹쳐 있어, 합쳐진 상태에서는 한 뭉치의
  테두리 4개만 읽힌다. **합쳐진 상태에서 8장의 테두리가 보이도록 두 뭉치의 카드가 번갈아 쌓여야 한다**
  (짝 1 · 홀 1 · 짝 2 · 홀 2 …). 이것과 D-027의 뒷면 텍스처는 같은 문제(겹친 카드가 구분되지 않는 것)의
  두 처방이므로 함께 만든다.
- **주사위 눈이 바뀌는 속도를 늦춘다.** 현재는 스크램블 구간에서 실제 눈 배치 세 가지가 빠르게 지나간다.
  단계 수(두세 번)는 그대로 두고 각 단계가 머무는 시간을 늘려 눈이 읽히게 한다.
- [도출] 주사위도 시간을 조정할 수 있어야 하면 연출 테스트 페이지의 슬라이더로 노출한다 — 덱과 같은 방식이며
  기본값은 사용자가 보고 정한다.

## D-029 · 2026-08-14 · 홀·짝 뭉치는 리플처럼 엮여야 한다 (쌓임 순서 불변식)

> 홀수 레이어와 짝수 레이어가 섞일때 서로 엮여야해. 바닥부터 숫자를 셀때, 1357과 2468이 섞이는데 3이 8 위로 올라가거나 하면 안돼. 3은 2와 4 사이로 가야해.

**도출된 계획**

- **쌓임 순서 불변식**: 카드는 바닥부터 1~8번이고, 홀 뭉치(1·3·5·7)와 짝 뭉치(2·4·6·8)가 합쳐질 때
  **원래 순서로 한 장씩 엮여야** 한다. **뭉치 단위로 통째로 위나 아래가 되면 실패다** — 그래서 지금
  화면에 4장만 보인다(한 뭉치의 테두리 4개만 읽힌다).
- **카드가 옮겨 갈 수 있는 범위는 자기 이웃뿐이다.** 3은 2와 4 사이에 있어야 하고, 8 위로 올라가면 안 된다.
  즉 합쳐진 뒤의 순서는 `1,2,3,4,5,6,7,8` 또는 인접한 쌍의 상하만 뒤바뀐 `2,1,4,3,6,5,8,7` 꼴이며,
  그 밖의 재배열은 허용되지 않는다.
- [도출] 따라서 **z 순서는 뭉치 단위가 아니라 카드 단위**여야 한다. 지금은 뭉치 하나에 z가 하나씩 걸려
  있어 한 뭉치가 통째로 다른 뭉치를 덮는다.
- [도출] D-025의 「맨 위 카드가 좌우 번갈아 지나간다」는 이 불변식 안에서 이렇게 읽는다 — 매 사이클마다
  **인접한 쌍(홀 n, 짝 n+1)의 상하 관계가 뒤바뀌어** 맨 위에 오는 뭉치가 번갈아 바뀐다. 전체 순서가
  뭉치별로 뒤집히는 것이 아니다.
- [도출] 이 불변식이 지켜지면 합쳐진 상태에서 **8장의 테두리가 모두 보인다**(D-028의 결함이 함께 해소된다).
  뒷면 텍스처(D-027)는 그 구분을 더 분명히 하는 처방이며 둘 다 만든다.

## D-030 · 2026-08-14 · 「좌우 번갈아」는 뭉치가 가는 쪽이 바뀐다는 뜻 (D-029 도출 정정)

> "맨 위 카드가 좌우 번갈아"는 처음에 1357이 왼쪽, 2468이 오른쪽으로 갔다면 그 다음에는 1357이 오른쪽, 2468이 왼쪽으로 가야한다는 뜻이야.

**도출된 계획**

- **분리할 때 각 뭉치가 가는 쪽이 사이클마다 뒤바뀐다.** 1번째 분리에서 홀(1·3·5·7)이 왼쪽·짝(2·4·6·8)이
  오른쪽이면, 2번째 분리에서는 홀이 오른쪽·짝이 왼쪽, 3번째는 다시 홀 왼쪽·짝 오른쪽이다.
- **supervisor의 앞선 해석을 취소한다.** D-029에서 이것을 「인접 쌍의 상하(z 순서) 교대」로 읽었으나
  그것이 아니다. 상하가 아니라 **좌우**다. z 순서에 관한 규칙은 D-029의 쌓임 순서 불변식뿐이며,
  그것은 사이클과 무관하게 항상 지켜진다(카드는 이웃과만 자리를 바꾼다).
- [도출] 원문 D-025의 「맨 위 카드가 중앙→오른쪽→중앙→왼쪽으로 지나간다」는 이 규칙의 결과다 — 맨 위
  카드가 속한 뭉치가 사이클마다 반대쪽으로 가므로 그 카드는 중앙에서 오른쪽, 중앙, 왼쪽 순으로 지나간다.
- [도출] 기울임도 가는 쪽을 따른다 — 왼쪽으로 가는 뭉치는 반시계 20°, 오른쪽으로 가는 뭉치는 시계 20°.
  뭉치의 좌우가 바뀌면 그 뭉치의 기울임 방향도 함께 바뀐다.

## D-031 · 2026-08-14 · 합쳐질 때만 아래 카드를 10ms 지연으로 추종

> 추가적으로, 덱이 벌어질때 말고 합쳐질때만 각 카드가  자기 아래의 카드를 10ms 딜레이두고 추종하게 할수 있을까?

**도출된 계획**

- **합치기 구간에만** 카드별 지연을 준다. **분리(벌어질 때)는 지금처럼 뭉치가 동시에** 움직인다.
- 지연은 **바닥에서 위로** 쌓인다 — 바닥 카드(1번)가 먼저 움직이고 그 위 카드가 **10ms 뒤**에 따라온다.
  카드 n의 합치기 시작 = 합치기 시작 + (n − 1) × 10ms. 8장이므로 마지막 카드는 70ms 뒤에 시작한다.
- [도출] **지연도 슬라이더로 뺀다**(「카드 추종 지연 ms」, 기본 10) — 모든 시간은 조정 가능해야 한다는
  D-025 규율에 맞춘다.
- [도출·사용자 결정 필요] 지금 합치기는 300ms이고 반복 대기가 0이므로, 마지막 카드가 70ms 늦게 시작하면
  그 카드의 합치기가 **다음 분리와 겹친다.** 둘 중 하나를 골라야 한다 —
  ①**구간이 70ms 늘어나는 것을 허용**한다(합치기 실질 370ms, 섞기 총 1,050 → 1,260ms) 또는
  ②**합치기 총 길이를 300ms로 고정**하고 각 카드의 지속을 230ms로 줄여 마지막 카드도 300ms에 끝나게 한다.
  ①은 사용자가 맞춘 템포가 조금 늘어나고, ②는 템포가 유지되지만 카드별 이동이 그만큼 빨라진다.
  **supervisor 제안은 ①**이다 — 추종이 눈에 보이려면 각 카드의 이동 시간이 유지되는 편이 낫고, 슬라이더로
  바로 되돌릴 수 있다.

**D-031 결정 (2026-08-14 사용자 확정)**: 위 두 갈래 중 **①**을 채택한다 — 합치기 구간이 **70ms 늘어나는 것을
허용**하고(합치기 실질 370ms, 섞기 총 1,260ms) 각 카드의 이동 시간 300ms를 유지한다. 템포가 마음에 걸리면
슬라이더로 되돌린다.

## D-032 · 2026-08-14 · 벌어짐·각도 슬라이더, 하단 윤곽선 얇게, 기본값 조정, 소멸 전 대기

> 1. 카드 좌우로 폭의 30%씩 벌리게했는데, 그거 슬라이드로 만들어.
> 2. 카드 틀어지는 각도도 슬라이드로 만들어.
> 3. 카드 하단 테두리의 검은 윤곽선 조금 더 얇게 만들어.
> 4. 추종지연 기본 20ms, 합치기 ms 기본 180ms로 해.
> 5. 완전히 합쳐진 이후 흩어지면서 사라지기 전까지 대기시간 슬라이더 만들어. 기본 100ms.

**도출된 계획**

- **슬라이더 셋을 새로 만든다** — 「벌어짐 %」(기본 30) · 「기울임 도」(기본 20) · 「소멸 전 대기 ms」(기본 100).
  앞의 둘은 지금 사양에 고정값으로 박혀 있던 것을 조정 가능하게 빼는 것이다.
- **기본값 둘을 바꾼다** — 「카드 추종 지연 ms」 10 → **20**, 「합치기 ms」 300 → **180**.
- [도출] 그에 따라 파생 타임라인이 바뀐다 — 합치기 실질 길이는 180 + (8−1)×20 = **320ms**이고, 마지막 합치기가
  끝난 뒤 **100ms 대기**한 다음 소멸이 시작된다. 소멸 자체는 500ms 그대로다.
- **카드 하단 테두리의 검은 윤곽선을 조금 더 얇게** 한다. 위쪽·측면 테두리와 카드 틀 자체는 그대로 두고,
  쌓인 카드의 아래쪽에 보이는 두꺼운 검은 선만 줄인다.
- [도출] 이 다섯은 매개변수 배선과 값 조정이라 supervisor가 직접 구현한다(별도 티켓 없음) — worktree·director
  준비 비용이 작업보다 크고, 값 확인은 실시간 간격 표집으로 즉시 된다. 근거를 여기 남긴다.

## D-033 · 2026-08-14 · 주사위 눈은 시간이 아니라 튕김을 따라 바뀐다

> 주사위 지금 몇단계 바뀌지? 주사위가 떨어지는동안엔 안바뀌고, 바닥에 닿아서 튕겨오를때마다 튕겨오른 50ms후에 1번만 바뀌게 해보자. 시간이나 그렇게 하지 말고. 지금 바닥에 1번 튕기는데, 2번 튕기게 하자. (처음은 크게, 두번째는 작게 튕기게.) -> 최초 떨어질때, 처음튕기고 50ms후 -> 두번째 튕기고 50ms후(이 숫자가 최종)

**현재 상태(질문에 대한 답)**: 눈은 **3단계**로 바뀐다 — 눈 패턴 세 벌이 「주사위 눈 체류 ms」(기본 220) ×3 =
660ms 동안 **시간 기준**으로 지나가고, **낙하 중에도 이미 바뀐다.** 튕김은 **1회**다(`die-roll`이 바닥 도달 뒤
−18px로 한 번 반등).

**도출된 계획**

- **눈 변경을 시간에서 튕김 이벤트로 옮긴다.** 낙하하는 동안에는 눈이 **바뀌지 않고 첫 값으로 고정**된다.
- **튕김은 두 번**이다 — 첫 번째는 크게, 두 번째는 작게.
- **눈은 각 튕김 뒤 50ms에 정확히 한 번씩** 바뀐다. 따라서 표시되는 눈은 셋이다 —
  ①낙하 중(첫 값) ②첫 튕김 + 50ms ③두 번째 튕김 + 50ms(**이 값이 최종**).
- [도출] 「주사위 눈 체류 ms」 슬라이더는 의미가 없어지므로 **없앤다.** 대신 「튕김 후 눈 변경 ms」(기본 50)와
  낙하·튕김 시간(주사위 낙하 ms 기본 300 · 첫 튕김 ms 기본 200 · 둘째 튕김 ms 기본 120)을 슬라이더로 둔다.
- [도출] 눈 변경 시점이 ms로 정해지므로 덱과 같은 방식(계산된 퍼센트 키프레임 주입)으로 구현한다 — 고정
  퍼센트 키프레임으로는 슬라이더 조정이 불가능하다.
- [도출] 최종 눈은 부르는 쪽이 넘긴 값이고 앞의 두 단계는 그것과 다른 눈 패턴이어야 한다(같은 값이 연달아
  나오면 바뀌는 것으로 보이지 않는다). 모션 줄이기에서는 지금처럼 최종 눈만 즉시 보인다.

## D-034 · 2026-08-15 · 요트 다이스 — 규칙 출처와 주사위↔점수판 화면 전환

> 닌텐도 세계게임전집에 있는 사양으로. https://namu.wiki/w/%EC%9A%94%ED%8A%B8(%EC%A3%BC%EC%82%AC%EC%9C%84)#s-2.1

> 요트 다이스는 주사위 - 점수판을 오고가야함. 로컬의 경우 설정한 인원수를 계속 빙글빙글 돌아야하고, 멀티일경우에도 타인의 턴을 지켜볼수 있어야함. 주사위 던지는 화면에서 하단의 점수판 핸들을 누르면 점수판이 튀어오르며 점수판 화면으로 전환. 굴리는중에는 점수판을 확인할수 있고, 굴리기가 끝나면 (3번 다 굴리거나 그만 굴리기를 선택할경우) 굴린 결과가 화면 상단 15%정도 영역에 고정되고, 점수판이 강제로 튀어나오면서 어느칸에 점수를 넣을지 선택하게 하자. 점수판의 각 항목을 한번 눌렀을때는 연한색으로 해당 칸에 적을시 몇점이 적히게 될지를 보여주고, 화면 하단의 '등록' 버튼을 누르면 실제로 점수가 적히고, 다음 플레이어의 턴으로 넘어가게끔.

**함께 확정된 것** (2026-08-15 사용자 선택)

- **요트 AI(봇)는 이번 범위가 아니다.** 요트는 상대 수를 읽는 게임이 아니라 확률 기대값 계산이라 기존
  미니맥스 탐색 코어를 쓸 수 없고, 사용자의 「단일 코어로 여러 게임에 적용 가능한 게 아니면 AI 부피를
  줄여라」 지시에 걸린다. 사람끼리 하는 모드를 먼저 내고 AI는 별도 티켓으로 미룬다.
- **M2는 한 트랙에서 순차로 간다** — 요트 다이스를 먼저 끝내고, 그 점수표·입력·저장 부품을 물려받아
  범용 점수판을 만든다. 두 게임이 같은 파일을 공유하므로 병렬 트랙으로 나누지 않는다.
- **범용 점수판의 범위는 아직 정하지 않았다** — 요트가 끝난 뒤 사용자에게 다시 묻는다.

**도출된 계획**: `docs/plan/yacht-dice.md`

## D-035 · 2026-08-15 · 함대 격침 — 클래식을 두고 변형룰을 따로 만든다

> 개인전 기반으로하고, 함대격침(클래식)을 두고 새 함대격침은 변형룰로 진행하자. 우선 기존 필드를 12*12로 확장하고, 미리 배 종류와 화력을 정해둔 프리셋중에 3개를 띄우고, 그중에 선택으로 고르게 하자. 적군 n명 기준으로 3턴동안 3, 4, 5, 6회 사격 계열 (턴당 최소 1사격, 최대 2사격), 모든 적군에게 같은 위치로 1회, 2회 사격, 턴당 1회 사격인 대신 기본 사격이 인접 4칸을 밝히는 예광탄, 인접 4칸을 같이 폭격하는 고폭탄, 피해를 주지 않는대신 적중한 칸을 밝히는 조명탄 턴당 1, 2, 3회 사격 등 다양한 사격 타입을 만들고, 배 타입도 다양하게: 추가장갑(배의 중앙 1칸이 2회 사격받아야 파괴됨), 잠수함(4칸 고정의 배로써 모든 부위 격침 전까지 격침여부 알리지 않음, 잠수함이 최후의 1척이 되면 위치 공개됨), 항공모함(배가 생존시 추가 화력을 지니는 대신 한대만 맞아도 항공모함의 배치가 알려짐), 상대에게 2턴마다 한번씩 고폭탄을 주는 대신 자신에게 매 턴 추가 예광탄 특성을 주는 1*1사이즈의 유리대포, 기타 클래식 배의 칸수와 갯수 조합으로 프리셋 만들기. 기본적으로 2칸배, 3칸배 2개, 4칸배 2개, 5칸 배를 가지고 공격 프리셋과 특수 배를 고르고 시작하는거지.

> 턴은 동시이나 연출은 순차. 각자 정한다음에 플레이어1 -> 플레이어 2 공격! 이라고 띄우고 hit! 혹은 miss! 띄워주는거지.

> 일반적인 hit/miss는 모두에게 그 좌표가 공개. 밝힌다는 모두에게 공개. 차라이 배 타입중에 간첩선(2칸*2척)을 추가하자. 매 턴 자신만 알수있는 조명탄 1발 발사, 간첩선 둘중 하나가 한대라도 맞으면 파괴.

> 관통탄 : 합격. 다만 연속한 2칸을 한번에 때리는걸로 하자.
> 연쇄사격 : 리턴이 너무 약함. 다른걸로 만들어야함. 그냥 빗나가버리면 무능력이나 다름없음. 무조건 유리대포나 간첩선과 조합이 강제됨.
> 차라이 랜덤샷 : 지정한 위치 외에 목표 플레이어의 보드중 아직 적중하지 않은곳에 랜덤 2회 추가사격 어떄? 너무 강한가? 밸런스 확인해봐.
> (밸런스 확인 결과 원안은 1.37배로 강해, 사용자가 **조준 1발 + 랜덤 1~2발, 이미 적중한 칸 제외**로 확정 — 실측 1.03배)

> 유리대포와 간첩선, 항공모함의 능력은 배가 침몰되기 전까지. 배가 침몰되면 없어지는거임. 유리대포의 경우 타 플레이어에게 주어지는 고폭탄도 없어짐. (물론 유리대포 고른 플레이어가 더 있다면 본인과 제3의 플레이어에게는 계속 지급됨. 이 점 코딩할때 유의할것)

> 예광탄의 인접 4칸은 조명탄이 맞아.
> 1. 2인의 경우에는 고폭탄과 확산탄 안쏘는턴에 일반탄 대신 조명탄 1발만 쏘기. 2. 2인의 경우 예광탄 C (매턴 예광탄 1발)만 스폰하도록. 3인 이상에서는 A와 B타입만 나오게. 3. 2인의 경우 동시사격 삭제, 연속사격이 3턴동안 5발 쏘도록. 4. 조명탄을 floor(2+enemy/3)으로 조정. 5. 새로운 사격 타입 2종 고안(2인에서 예광탄 1종이 줄고, 동시사격이 삭제되었으므로 탄총이 5가지밖에 안됨. -> 2총 추가 고안 필요, 모든 플레이어수에서도 나오도록). 6. 3인 이상 게임에서 특수 배 3중 택2 -> 4중 택2

**5번에 대한 답** — 관통탄(직선 연속 3칸, 매 턴 1회)과 연쇄 사격(좌표 4개 예약, 명중이 이어지는 동안만 발사)을 고안했다. 근거는 `docs/plan/fleet-strike.md` §3 「새로 고안한 두 종」.

> 새로운 배 타입: 종이배. 2*2 사이즈 1척 3*1 사이즈 1척, 조명탄에도 피격됨. (간첩선의 정찰에는 피격 안됨)

> 1. 사격 타입은 7종이 맞음.
> 2. 특수 배를 몇척을 허용할지 고민중. 1척만 하게 할까? 어떻게 하는게 밸런스적이나 재미적으로 좋을까?
> 3. 추가적으로, 2인 플레이의 경우에 플레이필드를 10*10으로 축소시키는 방안? (이경우 원양은 테두리 2칸 금지로 변경) 3인 이상이서는 서로 난사도 하고 하니까 금방 쑥대밭이 되지만, 2인이서는 화력에 한계가 존재.(특수탄 감안해야함.)

**2·3번의 답** (2026-08-15 사용자 선택 — 근거 수치는 `docs/plan/fleet-strike.md` §2 말미)

- **특수 배는 2척, 2인은 1척.** 무작위 3장 중 고른다(2인은 3장 중 1장).
- **2인 판은 10×10으로 축소**하고 원양은 테두리 2칸 금지로 바꾼다.

> 고폭탄과 확산탄은 턴당 1회. 연속사격은 3+enemy로 하고, 턴당 최소 1 최대 3으로 하자. 조명탄은 턴당 floor(2+enemy/2)로 하자 (당연히 기본 사격 1회는 이 횟수에 포함안됨). 예광탄은 매턴 예광탄 2발 버전과 매턴 예광탄 1발 + 조명탄 2발 버전으로 나누고, 고폭탄과 확산탄은 2턴에 한번씩만 쏠수 있게 하자. (안쏘는 턴은 일반탄 1발). 이렇게하면 총 6종이 되니 카드 3개 랜덤으로 선택해서 보여주고 그중 택1 하게 하면 될꺼야.
> 특수 배의 경우에는 추가장갑: 5칸배 1개를 추가하고, 두 5칸배를 추가장갑으로 업그레이드. 중앙 1칸이 2회 맞아야 부서지니 hit은 알리되, partial (부분 파괴)로 알려주자. 잠수함은 잠수함 추가이고, 맞던 안맞던 miss로 출력되게. 하지만 내부 상태는 miss(hidden hit)으로 처리. 항공모함은 5칸배 1개를 추가하는거고, 유리대포도 배 추가. 여러명이 유리대포 고르는경우에도 추가고폭탄 횟수 동일. 간첩선은 배 추가, 간첩선의 조명탄 사격은 그 여부도 알리지 않음. 그냥 말이 조명탄이지만 사실 상대의 원하는 칸 1칸을 직접 보는것. 보급선도 배 추가.

> 모든 적에게 같은 좌표로 사격하는거 이름을 동시 사격이라 하자. 추가적으로 확산탄: 3*3 영역을 왼쪽 위부터 읽는순서대로 숫자 붙일때, 1, 3, 7, 9에 일반탄, 5에 조명탄 사격. (조명탄 쏘고, 그 대각선 4칸에 일반탄 사격) 고폭탄과 같이 매우 강력한 탄종.

> 특수 배에 보급선도 추가. 4*2 사이즈이며 공격능력은 없지만 순수 생존력이 강함.
> 연안, 원양 태그 추가. 해당 태그는 보유한 배 중 랜덤한 2척에 붙으며 붙은 배는 연안: 가장자리 2줄에만 배치, 원양: 가장자리 3줄에 배치 불가. 무조건 연안 1척과 원양 1척을 보유하게 됨. -> 기본적인 정보 제공. 태그가 없는 배는 자유배치 가능. 어떤 배에 태그가 붙을지는 프리셋을 정한 이후에 결정됨. (유저마다 다르게 지정.)
> 조명탄은 기본 사격을 대체하지 않는 추가사격임.

**함께 확정된 것** (2026-08-15 사용자 선택)

- **배가 전멸하면 탈락하고 최후 1인이 이긴다.**
- **유리대포가 상대에게 주는 고폭탄은 2턴마다 모든 상대에게** 간다.
- **항공모함의 추가 화력은 턴당 사격 1회 추가**다.

**도출된 계획**: `docs/plan/fleet-strike.md`

## D-036 · 2026-08-15 · 바둑 — AI 없이 심판만

> 바둑의 경우에는 ai 없이, 대국중에 사석 정리하고 대국 끝났을때 집 계산만 해주면 됨.

**함께 확정된 것** (2026-08-15 사용자 선택)

- **계가는 한국식**이다 — 죽은 돌을 걷어낸 뒤 각자 둘러싼 빈 점 수에 잡은 돌 수를 더해 비교하고,
  뒤에 두는 백이 **덤 6.5집**을 미리 받아 비김이 없다.
- **종국에 양쪽이 표시한 죽은 돌이 다르면 대국을 재개한다** — 판으로 돌아가 실제로 잡아 보인다.
  온라인 바둑 서버들의 표준이고 분쟁이 남지 않는다.

**도출된 계획**: `docs/plan/baduk.md`

## D-037 · 2026-08-15 · 구조 예산은 현안보다 15% 넉넉하게

> 구조 예산은 항상 조금 더 넉넉하게 줘. 폭주하지 않을정도로만 제어하면 돼. 현안보다 15%정도 더 많이 주면 될꺼야.

**도출된 계획**

- **줄 수 예산의 상한은 최소한 「현재 실측 × 1.15」로 잡는다.** 이미 그보다 넉넉하면 내리지 않는다 —
  이 지시는 여유를 늘리라는 것이지 조이라는 것이 아니다.
- 상한의 목적은 **모델이 스스로 범위를 부풀리는 폭주를 막는 것**이지, 지시받은 기능에 필요한 증가를
  막는 것이 아니다(D-004와 같은 취지). 티켓마다 실측을 다시 재고 상한을 갱신한다.
- [도출] **직접 의존성 개수에는 적용하지 않는다.** 4개를 4.6개로 만들 수 없고, 의존성 하나는 줄 수와
  달리 공급망 판단이 필요한 별개의 결정이다. 지금처럼 4개를 유지하고 필요할 때 개별로 재판단한다.
- [도출] 계기가 된 사건: M2-YACHT-1 병합 직후 `src` 비테스트가 **1,999 / 2,000줄**이 되어 남은 여유가
  한 줄이었다. 폭주 제어가 아니라 다음 티켓을 막는 상태였다.

## D-038 · 2026-08-15 · 함대 격침 사격 타입 밸런스 목표와 확정치

> 밸런스 목표: 2인 밸런스와 다인 밸런스로 구분, 고정 5발 연속사격 대비 모든 타입이 0.8~1.3사이에 있으면 됨. goal으로 만들어. 밸런스에 문제가 있을때 조정하는법은 수치 조정 (일반탄 -> 조명탄 등 탄종 변경), 추가 탄 제공(추가 조명탄 제공 등), 인원에 따른 계수 조정 등.
> 다인 밸런스는 일단 3인에서 맞으면 되는걸로.

> 확산탄 왜 다 못때린다는거지? 때린대 또 때리는거 감수하면 무조건 다 채울수 있잖아. 극단적으로 1번자리만 쓴다고 해도, 전부 채울수 있어. (확산탄등 범위칸을 위해서 플레이필드에 2짜리 패딩 두를 필요는 있음)

> 3인 고폭: 쉬는 턴 일반탄->조명탄으로 너프해.
> 4인 이상 관통탄 : 매 턴 관통탄 1발 + 조명탄 1발
> 4인 이상 산탄 : 5*5에 6발, 중앙에 3발.

> 연속사격 너프. 3턴간 2~3인 5발, 4인 이상 6발.
> 동시사격은 그냥 생각해봤는데 무조건 삭제가 맞음. 개인에게 돌아오는 리턴이 없음.
> 예광탄 A -> 삭제 혹은 너프 필요. (매턴 예광탄 2발쏘는거 : 일반탄 2발의 상위호환)
> 확산탄 추가버프안: 확산탄은 안쏘는턴에 일반탄+조명탄 발사. (다이아몬드형 선택은 측정 밖)
> 예광탄의 인접 4칸은 조명탄이 맞아.

> 항모는 아군 배 3척 침몰까지 하는건 어떨까 고려해봐. 배는 2개나 고를수 있으니 조금 밸런스 안맞아도 되긴 하는데, 너무 능력이 허무하게 잃어지는거같아서 그래.
> 항모는 아군3척으로 하고,

> 산탄 룰에 대해 햇갈리는거같은데, 한번 사격시 산탄은 떨어진곳에는 또 떨어지지 않아. 총 사격 발수는 보장돼. 그리고 중앙 3발에 외곽 3발, 중앙 2발에 와곽 4발 등으로 착탄보장 -> 무조건 중앙에 몇발, 외곽에 몇발로 정하자.

> 4인 이상에서 연속사격을 무조건 3턴 5발로 제안하는건은?
> 적용해봐. 근데 산탄은 2인에서는 맵 작아지는데 5/2가 괜찮아? 3인 이상부터 6/3은 어때?

**최종 확정** (2026-08-15) — **연속 사격은 인원 무관 3턴 5발**이다. 그러면 3인 이상은 판·함대·카드가
전부 같아져 **밸런스가 인원과 무관**해지고, 4인 이상 전용 보정(관통탄 +조명탄, 산탄 6/3)이 사라진다.
**산탄도 인원 무관 6발/중앙 3발**이다 — 실측 결과 판 크기와 무관하게 6발이 맞았다(5발은 양쪽 다 낮음).

**도출된 계획**: `docs/plan/fleet-strike.md` §3과 §3.6(밸런스 실측)


## D-039 · 2026-08-15 · 「범용 점수판」은 게임이 아니라 공통 부품이다

> 범용 점수판이라고 내가 지시한건, 점수판 시스템 (하단에 점수판 핸들,플레이중에 원할때마다 점수판 열람 가능 등)을 다른 게임에서도 쓸수있게 (텍사스 홀덤에서 점수판을 처음부터 새로 만들지 않게) 하라는거였어.

**정정된 것** — 계획서와 파생 문서 전부가 이것을 **게임 목록의 한 칸**(T0 8종 중 6번, 「인원 제한없음 ·
점수판·공용 모드」)으로 잘못 적고 있었다. 사용자 의도는 **재사용 가능한 UI 부품**이다.

**확정된 것** (2026-08-15 사용자 선택)

- **게임 목록에서 뺀다.** T0은 **7종**이 된다 — 사목·오목·육목·리버시·요트 다이스·함대 격침·체스.
- **점수판 시스템은 공통층 부품**이다. 선공 결정(D-023)·연출 3종과 같은 자리이며,
  카탈로그에 등록하면 따라오는 구조여야 하고 게임별 코드에 복제하지 않는다.
- 사양 정본은 `docs/plan/yacht-dice.md` §3(하단 핸들 · 플레이 중 열람 · 종료 시 강제 팝업 ·
  칸 미리보기 · 「등록」 확정)이다.
- **M2는 요트 다이스로 사실상 끝났고**, 남은 것은 그 점수판을 `YachtGame.tsx`에서 빼내
  부품으로 만드는 일이다. 「범용 점수판의 범위를 정한다」(D-034)는 질문은 **잘못된 질문이었으므로 폐기**한다.


## D-040 · 2026-08-15 · 요트 기록은 탭을 닫으면 사라진다

> 복원? 크롬탭이 열려있는한 데이터 유지 아니야? 크롬탭 닫으면 삭제이고. 앱 전환해도 남아있는지 확인이 필요하다는건가?

> 필요없지 않을까? 매 판이 수시간인것도 아니고, 탭을 닫으면 사용자가 명확히 초기화하겠다는 의사를 표한거라고 생각하는데.

> 꺼.

**확정** — 요트의 기록 저장을 `localStorage`에서 **`sessionStorage`로 바꾼다.**

- **탭을 닫으면 기록이 사라진다.** 그것을 사용자가 그 판을 그만두겠다고 밝힌 것으로 본다.
- 탭이 살아 있는 동안에는 유지된다 — 화면을 껐다 켜기, 앱 전환, 새로고침 전부 통과한다.
- [도출] M2의 완료 조건이 바뀐다. 「앱을 완전히 종료했다 켜서 복원」은 이제 성립하지 않으며,
  **「탭을 유지한 채 앱 전환·새로고침을 거쳐도 그대로」 + 「탭을 닫으면 비워진다」**가 판정 기준이다.
- [도출] 이 결정은 요트에만 적용된다. **점수판 시스템은 공통 부품이므로 저장 방식은 그것을 쓰는
  게임이 각자 정한다**(D-039) — 부품에 저장 정책을 박지 않는다.
- [기록] 유지하자는 근거는 하나 남아 있었다 — 탭이 사라지는 이유 중 사용자 의사가 아닌 것들
  (크롬이 메모리 압박으로 백그라운드 탭을 버림 · 무거운 앱을 쓰는 동안 크롬이 통째로 죽음 · 배터리 방전).
  사용자가 그것을 알고 끄기로 정했다.

## D-041 · 2026-08-15 · 요트 실기기 검수 5항 — 주사위 선택·확정 문구·굴림 애니메이션·점수판 레이어·뒤로가기

> 1. 리롤 여부는 주사위를 눌러서 정함. 주사위 선택 버튼은 삭제. 리롤할 주사위는 위로 주사위폭의 70%만큼 올라가고, 15도 회전함. (회전과 이동 동시에, 애니메이션은 spring, 70ms 지속) 선택 취소시에는 같은 애니메이션으로 제자리에 돌아옴.
> 2. 다시 굴리기가 취소, 그만 굴리기가 확정의 의미로 각각 좌/우라면 다시 굴리기와 확정! 으로 멘트를 바꿔야함.
> 3. 고정한 주사위는 다시 굴리는 애니메이션도 나오면 안됨. 지금은 뭘 고정했건 5개 다 굴리는 애니메이션 나옴.
> 4. 점수판 올리기하면 화면 상단 15%만 남기고 점수판이 기존 요소 위로 올라와야해. 이때 화면 상단 15%는 게임마다 특화된 UI(요트다이스의 경우에는 현재 주사위)로 바뀌어야하고. 지금은 기존요소는 그대로있고 밑에만 점수판이 붙는 형태야.
> 5. 지금은 어느단계던 뒤로가기를 누르면 탭에서 나가짐. 대국 -> 방 로비 -> 타이틀과 게임 목록 -> 방 로비 -> 타이틀 돌아가는 루트가 필요함.

**함께 발견된 것** (2026-08-15 supervisor 브라우저 검수)

- **게임 목록의 요트 이름이 「Yacht Dice」로 영어다.** 사목·오목·육목·리버시는 한글이므로 **「요트 다이스」**로 고친다.

**통과한 것** (같은 검수) — 그만 굴리기 시 점수판 강제 팝업과 핸들 잠금 · 주사위 상단 고정 ·
12칸이 세계게임전집 사양(스리 다이스 없음) · 칸 미리보기 값 정확(2+3+5+2+1=13) ·
등록 버튼 활성 조건 · 등록 후 차례 전환 · 선공 결정 화면 · `localStorage` 미사용 ·
새로고침 뒤 「저장된 경기 열기」로 복원 · 탭을 닫으면 초기화(D-040 확인).

## D-042 · 2026-08-15 · 점수판 결함 3항 — 열 매핑·상시 예상 점수·빈칸 표기

> 점수판 결함도 있어.
> 1. 내가 1p인데, 3p자리에 입력이 됨.
> 2. 사양변경이긴한데, 그냥 모든 빈 칸에 회색으로 받을수 있는 점수 상시 표기. 누르면 그거만 노란색으로 하이라이트 표기되게. 이후 확정으로 기입.
> 3. 점수판의 공란은 그냥 공란으로 둬. ー로 두지 마.

**1번의 원인 진단** (supervisor, 코드 실측)

입력 자체는 정확했다 — 그때 차례가 「3P」였고 그 사람 칸에 들어갔다. 거짓말을 한 것은 표시다.

- `App.tsx:309` 로컬 참가자 이름은 만들 때 붙는다 — 1번은 사용자 이름, 나머지는 `2P`·`3P`·`4P`.
- `App.tsx:301` 3인 이상이면 주사위로 차례 순서를 정하며 **참가자 배열 자체를 그 순서로 재정렬**한다.
- `YachtGame.tsx:46` 열 색이 `player-${participants.indexOf(participant)+1}`, 즉 **정렬 뒤의 위치**를 쓴다.
- 그래서 이름 「3P」가 **1P 색을 달고 첫 열**에 오고, 이름의 숫자와 색과 위치가 전부 따로 논다.
  게다가 **어느 열이 자기인지 표시가 없어** 사용자 이름이 뒤로 밀리면 자기 열을 찾을 수 없다.

**확정된 처방**

- **색은 좌석에 고정한다.** 참가자를 만든 순서로 색을 주고, 열 순서만 차례순으로 둔다.
- **열 순서도 좌석 순서로 고정하고** 현재 차례는 하이라이트로만 표시한다 — 자기 열이 늘 같은 자리에 있어야 찾는다.
- **자기 열에 표시를 단다**(굵은 테두리 또는 「나」 배지).
- [도출] 이 셋을 다 하면 이름·색·위치가 일치한다. 로컬 이름을 `2P`·`3P`처럼 순번으로 붙이는 것 자체는
  순서를 고정하면 문제가 되지 않으므로 그대로 둔다.

**2번·3번 — 점수판 표기 사양 변경**

- **현재 차례인 사람의 아직 비어 있는 칸에는 받을 수 있는 점수를 회색으로 상시 표기**한다.
  칸을 눌러야 보이던 미리보기를 없애고 전부 미리 보여준다.
- **누른 칸만 노란색으로 하이라이트**되며, 그 뒤 「확정」으로 기입한다.
- **그 밖의 빈칸은 `—`가 아니라 그냥 빈칸**으로 둔다.
- [도출] 이 변경은 D-039로 공통 부품이 된 **점수판 시스템의 사양**이므로 뒤에 오는 게임이 그대로 물려받는다.

## D-043 · 2026-08-15 · 함대 격침 — 클래식 순정 사양과 화면 구성

> 배틀쉽(클래식)과 배틀쉽도 구현에 넣어. 순정사양은 보드 9×9, 배 2,3,3,4,5
> 배틀쉽 다 만들고나서 수정 완료된 점수판으로 각 플레이어 남은 배 수, 탄 종류등을 표기하는걸 추가로 붙일꺼야.
> 배틀쉽 화면 구성은 16:9 비율의 화면을 항상 상단, 중단, 하단으로 나누고(각각 세로의 45, 10, 45) 좌, 중, 우로 나누고 (각각 가로의 15, 70, 15)
> 상단부터
> 조준 페이즈
> 좌: 이전 플레이어 보드 보기, 우: 다음 플레이어 보드 보기, 중앙: 현재 보고있는 적 플레이어 보드
> 좌 우 눌러서 넘길때는 캐로셀마냥 이미 보드는 다 그러져있고, 누르면 넘어가게끔.
>
> 배치 페이즈
> 내가 지니고있는 배 현황, 내가 지닌 탄종의 특성 표기
>
> 중앙 영역은
> 배치 페이즈에는 배 선택 및 회전, 조준 페이즈에는 탄 선택 및 배치.(관통탄의 경우는 회전을 넣기보단 세로2×1, 가로 1×2 2종류의 탄종중 1개 선택으로 하자) 탄 선택할때 이중에 1개 고를수 있다 등의 선택풀을 표기하기위해 택1 탄종을 박스로 감싸기. 박스 1개 = 발사 1번
>
> 하단 영역
> 중앙: 항상 내 배치 표기. 그 상단에는 내 탄종에 대한 설명 표기.
>
> 배치 페이즈에는 하단을 눌러서 배를 배치.
> 조준 페이즈에는 상단을 눌러서 탄 배치.

**확정된 것**

- **클래식의 순정 사양은 9×9 판에 배 다섯 척 2·3·3·4·5칸(17칸)**이다. 종전 문서의 「10×10에 5·4·3·3·2」를 대체한다.
- **클래식과 변형 둘 다 구현 대상**이다.
- **점수판 시스템으로 각 플레이어의 남은 배 수와 탄 종류를 표기하는 것은 배틀쉽을 다 만든 뒤에 붙인다** —
  D-039의 공통 부품이 실제로 재사용되는 첫 사례다. 이번 범위가 아니다.
- **화면은 세로 45/10/45, 가로 15/70/15의 아홉 칸으로 항상 나눈다.**
- **상단** — 조준 페이즈에는 좌가 이전 플레이어 보드, 우가 다음 플레이어 보드, 중앙이 지금 보고 있는 적 보드다.
  좌우로 넘길 때는 **캐로셀처럼 보드가 이미 다 그려져 있고 누르면 넘어간다.**
  배치 페이즈에는 내가 가진 배 현황과 내가 가진 탄종의 특성을 표기한다.
- **중단** — 배치 페이즈에는 배 선택과 회전, 조준 페이즈에는 탄 선택과 배치.
  **관통탄은 회전 대신 세로 2×1과 가로 1×2 두 종류 중 하나를 고르는 방식**으로 한다.
  택1 탄종은 **박스로 감싸** 선택풀임을 보이고, **박스 하나가 발사 한 번**이다.
- **하단** — 중앙에 항상 내 배치를 표기하고 그 위에 내 탄종 설명을 둔다.
- **배치 페이즈에는 하단을 눌러 배를 놓고, 조준 페이즈에는 상단을 눌러 탄을 놓는다.**

## D-044 · 2026-08-15 · 함대 격침 화면 — 미정 6항 확정과 보드 이름표

> 1. 2인 캐로셀: 빼.
> 2. 탈락보드: 잔여.
> 3. 다른적에게 쏠수 있음. 중앙에 모든 발 회수라는 버튼 눌러서 다른 플레이어 탭으로 넘어가지 않아도 배치 초기화 가능하게끔 해.
> 4. 중단은 좌 중 우 구분 없이 다 사용 가능.
> 5. 하단영역의 위쪽을 말함.
> 6. 배의 우하단에 꼬리표로 남기고, 해당 배를 눌렀을때 배치 불가 영역이 점멸하게끔.
>
> 추가적으로 캐러셀 배치네서 각 보드의 좌상단에는 누구의 보드인지 표기가 있어야함.

**확정된 것**

- **2인에서는 캐로셀을 두지 않는다.** 적이 하나뿐이라 이전·다음이 뜻이 없다.
- **탈락한 사람의 보드는 캐로셀에 남긴다.**
- **한 턴에 여러 발을 서로 다른 적에게 나눠 쏠 수 있다.** 그리고 중앙에 **「모든 발 회수」 버튼**을 두어,
  다른 플레이어 탭으로 넘어가지 않고도 그 턴의 탄 배치를 통째로 되돌릴 수 있게 한다.
- **중단 영역은 좌·중·우 구분 없이 가로 전체를 쓴다.** 아홉 칸 분할 중 중단만의 예외다.
- **하단의 「그 상단」은 하단 영역(세로 45%) 안쪽의 위쪽**이다. 중단 영역이 아니다 —
  중단은 탄 선택 용도이므로 겹치지 않는다.
- **연안·원양 태그는 배의 우하단에 꼬리표로 표시**하고, **그 배를 누르면 배치할 수 없는 영역이 점멸**한다.
- **캐로셀의 각 보드 좌상단에 그 보드가 누구 것인지 표기**한다.


## D-045 · 2026-08-15 · 함대 격침 화면 — 세로 기준, 빈 칸은 공란

> 1. 세로화면 기준.
> 2. 언급 안된 칸은 공란으로. 나중에 쓸모가있을지도 모르긴 함.

**확정** — 함대 격침 화면 사양이 이것으로 닫힌다.

- **세로 화면(9:16)이 기준**이다. D-043 원문의 「16:9 비율의 화면」은 폰 화면을 가리킨 것이며 가로가 아니다.
  세로에서는 정사각 보드가 중앙 70% 폭을 100% 채우고 상·하단 높이의 88%를 쓴다.
- **아홉 칸 중 용도를 적지 않은 칸은 공란으로 둔다** — 배치 페이즈의 상단 좌·우, 두 페이즈의 하단 좌·우.
  지금은 비우되 나중에 쓸 자리로 남긴다.


## D-046 · 2026-08-15 · 작업 카드 상한을 동시 7장으로

> worker 상한은 3장이 아니야. director 재량에 따라 동시 최대 7장까지 가능하게 해.

**확정** — D-007의 「작업 카드는 최대 3장」을 **동시 최대 7장**으로 올린다. 몇 장을 띄울지는 **director 재량**이다.

- [도출] 제어면의 동시 세션 상한은 root 하나당 **9**다(`control.py`의 `MAX_LIVE`).
  worker 7 + director 1 = 8이라 그 안에 들어간다.
- [도출] **상한이지 목표가 아니다.** 같은 파일을 여럿이 건드리면 병합이 깨지므로,
  독립적으로 나눌 수 있을 때만 늘린다. director가 순차로 가기로 판단하면 그것도 맞다.
- [도출] 이미 돌고 있는 세션의 부팅 문서에는 옛 상한이 박혀 있다 — PREFLIGHT 변경은
  **다음에 뜨는 세션부터** 적용된다.

## D-047 · 2026-08-15 · 요트 2차 실기기 검수 8항

> 1. 지금 주사위 굴리고나서 리롤 포지션에 가있어. 굴리면 확정 포지션으로 굴려져야해.
> 2. 애니메이션이 즉각인걸로 보여. 애니메이션 시간이 반영되었는지 확인해.
> 3. 로컬에서는 (나) 가 필요없음.
> 4. 선수 정하는 애니메이션 나올때, 결과가 뜨고 읽을틈도없이 바로 게임이 시작해. 4초 뒤 페이드아웃으로 사라졌으면 좋겠어.
> 5. 보너스 식스와 초이스 사이에 있어야해.
> 6. 주사위 굴릴때 점수판 안열었는데도 점수판이 열리곤 해.
> 7. 뒤로가기 하면 탭에서 나가짐. 요트에서는 점수판 상태 -> 게임 화면 -> 방 로비 -> 타이틀 순으로 나가져야해. 이 기기에서 플레이의 경우에는 방 로비 대신에 게임 목록.
> 8. nollawa 타이틀에서 부제목으로 사목에 대한 이야기가 나와. 그걸 "친구와 즐기는 파티게임"으로 고쳐야해.

**2번 확인 결과** (supervisor, 코드 실측) — **애니메이션 시간은 반영돼 있다.**
`styles.css`에 `transition: transform 70ms cubic-bezier(.2, 1.4, .4, 1)`과
`transform: translateY(-70%) rotate(15deg)`가 있어 70ms·spring·70%·15도가 전부 사양대로다.
즉각으로 보이는 것은 **70ms가 60fps 기준 4프레임**이기 때문이다. 사양에 적힌 값이라 임의로 바꾸지 않는다 —
늘리려면 사용자가 새 값을 정해야 한다.

**2번의 답** (2026-08-15 사용자 결정) — **선택 애니메이션을 70ms에서 120ms로 늘린다.**
70ms는 60fps에서 4프레임이라 눈에 안 보였다. 120ms면 약 7프레임이다.
이동 70%·회전 15도·spring 곡선은 그대로다.

**7번은 재작업이 아니라 미완이다.** D-041 5번으로 뒤로가기 라우팅을 구현했고 커밋(0bb4cf3)까지 있었는데
실기기에서 여전히 탭이 닫힌다. 이번에는 경로가 더 자세해졌다 —
**요트: 점수판 상태 → 게임 화면 → 방 로비 → 타이틀**, 그리고 **「이 기기에서 플레이」면 방 로비 대신 게임 목록**.

**나머지 확정**

- **굴린 직후 주사위는 확정 포지션(기본 위치)이어야 한다.** 지금은 리롤 선택 포지션(올라간 상태)으로 굴려진다.
- **「나」 배지는 로컬 플레이에서 띄우지 않는다.** 혼자 조작하므로 뜻이 없다.
- **차례 순서 결정 결과는 4초 동안 보이고 페이드아웃으로 사라진다.** 지금은 읽을 틈 없이 게임이 시작된다.
- **상단 보너스 행은 식스와 초이스 사이**에 온다. 지금은 표 맨 아래에 있다.
- **주사위를 굴릴 때 점수판이 저절로 열려서는 안 된다.** 강제로 여는 것은 굴리기가 끝났을 때뿐이다.
- **타이틀의 부제목을 「친구와 즐기는 파티게임」으로** 바꾼다. 지금은 사목 설명이 나온다.

## D-048 · 2026-08-15 · 함대 격침 시각 사양 — 피격 표기와 배 텍스처

> hit: 빨강 x자
> miss: 검정 x자
> partial: 작은 빨강 x자
> 배는 5가지 텍스쳐 합쳐서 만들자. 배 몸통, 배 선두, 배 후미, 2×2등에서 배 코너, 배 몸통(대)
> 보급선(2×4)는 배 코너와 배 몸통만으로 만들고
> 기본적으로 높이의 90% 지점에 선을 하나 긋고, 그걸 배 몸통(대)으로 정의.
> 높이의 90%와 10%에 선을 그으면 그게 배 몸통.
> 중앙에 반지름이 40%인 오른쪽 반원을 그리고, 그로부터 왼쪽으로 높이 90%지점과 높이10%인 지점에 선 그으면 배 선두, 반원 대신의 높이가 80%인 반 정사각형 가온데에 그리고 비슷하게 10%지점과 90%지점 이어서 그리면 배 후미. 배 코너도 비슷하게 작성.
> 배는 아주 옅은 회색으로 칠하자. (노랑은 항상 선택에 관련된 하이라이트 색이므로 배에는 사용 금지, 현재 배치중인 배 커서를 노랑으로 하는건 괜찮음)

**도출된 계획**: `docs/plan/fleet-strike.md` §4.8

## D-049 · 2026-08-16 · 검수 33건 RCA의 재발방지 — 미관측은 RED, 카드 형식 6칸, 완료 표시 시점

> 재발방지를 어디까지 채택할까요? → **미관측을 RED로**
> 새 카드 형식(WHY·REUSE·FORBID·SHOW-ME·PAIRS·TOUCHES·OPEN)을 도입할까요? → **전부 도입**
> 구현 상태 표를 언제 「완료」로 바꿀까요? → **사용자 검수 통과 후에만**

**배경** — 함대 격침 티켓 넷을 돌린 뒤 실기기 검수에서 결함 33건이 나왔다. 33건 중 리듀서 규칙
오류는 **0건**이고 FALSIFIER 46항이 잡을 수 있었던 것도 **0건**이었다. RCA의 공통 상위 원인은
**모든 검증 채널이 생산자 자신의 프레임 안에서 도는 대리 지표였고 파이프라인이 대리-초록을 완료로
처리한 것**이다. 근거·대조군·수치는 `collab13/reports/rca-fleet-33-process-20260816.md`와
`rca-fleet-33-ticket-form-20260816.md`에 있다.

**확정된 것 셋**

1. **미관측은 GREEN이 아니다.** 결함 표면에 렌더링 산출물이 포함되는 티켓은 워커가 신고한
   물리기기·브라우저 관측 항목에 미관측이 남아 있으면 **완료로 닫지 않고 검수 대기로 둔다.**
   워커는 이미 그것을 정직하게 신고하고 있었고 바뀌는 것은 supervisor의 판정뿐이다.
2. **작업 카드에 여섯 칸을 더한다** — `WHY`·`REUSE`·`FORBID`(여기에 요구를 쓰면 그 카드는 무효) ·
   `SHOW-ME`(상태를 만들고 화면을 찍으면 이렇게 보인다 + 판정자는 구현자가 아니다 · 스크린샷 없으면
   그 번호는 완료가 될 수 없다) · `PAIRS`(겹침·정원·순서 조항의 반대 방향을 함께 적는다) ·
   `TOUCHES`(건드리는 공유 표면) · `OPEN`(사양이 침묵하거나 모순인 지점 — 여기 적힌 것은 DONE-WHEN에
   넣지 않고 되돌린다). result 카드에는 `SPEC-CONFLICT:`를 더한다.
   **FALSIFIER에서 소스 문자열 검사 조항은 전부 뺀다** — 통과 방법이 grep이라 실제로 grep 테스트가 됐다.
3. **구현 상태 표는 사용자 검수를 통과한 뒤에만 「완료」로 바꾼다.** 구현 티켓이 스스로 완료로 바꾸면
   다음 티켓의 POPULATION 분모가 0이 된다. 검수 전에는 **「구현됨 · 미검수」**로 둔다.

- [도출] 채택하지 않은 후보(소스 문자열 단언 제외 · 컴포넌트 1회 실행 · 줄 길이 린트 · 사양 모순 검사 ·
  대체된 결정 문장 삭제 · 완료 기능 교차 티켓 책임)는 위 보고서에 근거와 함께 남아 있다. 필요해지면 그때 꺼낸다.

## D-050 · 2026-08-16 · 검수 결정 5건 — 조명탄 표기·꼬리표 병기·배치 미리보기·회전 기준점·승패 표시

> 1. 조명탄이 밝힌거는 검정색 네모로 나타내자. 배 있으면 안이 검정색으로 채워진 네모 (칸 크기의 60%), 아니면 흰색 네모. (같은 크기)
> 2. 배 꼬리 표기 유지. 다만 연안 원양 태그는 배꼬리 표기를 대체하면 안됨. 병기하는쪽으로 개발해야함
> 3. 마치 오목에서 둘 공간을 표기하는거마냥 반투명하게 배 모양을 띄워주기.
> 4. 중앙으로. 짝수면 꼬리에 가까운칸.
> 5. 패배한 사람의 보드에 크게 패배(혹은 탈락)라고 적기, 최후의 승자의 보드에는 승리라고 적기.

**확정된 것**

- **B-4 · 조명탄이 밝힌 칸은 네모다.** 크기는 **칸의 60%**로 두 경우가 같고, **배가 있으면 안이 검정으로
  채워진 네모**, **없으면 흰 네모**다. 실제로 쏘아 맞힌 명중(빨강 ×)·빗나감(검정 ×)과 모양 자체가 다르므로
  구분된다. [도출] 흰 네모는 테두리가 있어야 빈 칸과 구별된다.
- **B-19 · 배 꼬리의 길이 표기는 유지한다.** 그리고 **연안·원양 태그가 그것을 대체해서는 안 되고 병기**한다 —
  지금은 태그 꼬리표가 길이 숫자와 같은 자리에 겹쳐 있다.
- **B-22 · 배치 미리보기는 오목이 둘 자리를 표시하듯 반투명한 배 모양**을 띄운다. 색은 D-048의
  「배치 중인 배 커서는 노랑이어도 된다」가 그대로 적용된다.
- **B-23 · 회전 기준점은 배의 중앙**이다. 칸 수가 짝수면 **꼬리에 가까운 칸**을 기준으로 삼는다.
- **B-26 · 패배한 사람의 보드에는 크게 「패배」(또는 「탈락」), 최후의 승자 보드에는 「승리」**를 적는다.

**아직 정해지지 않은 것**

- **격침 표기**(B-5·B-25). D-2는 「밝혀진 칸 2종 + 격침」 셋을 물었는데 답은 앞의 둘만 왔다.
  격침된 칸과 격침된 배를 명중(빨강 ×)과 어떻게 구분할지가 여전히 사양에 없다.

## D-051 · 2026-08-16 · 계층 순차 대조와 플레이어 입력 전수조사

> 내용 한번 더 검증해. 내 최종 원안 -> 초기 개발 스펙 -> 초기버전 구조 -> 초기버전 버그 및 피드백 (아직 안밝혀진 잠재적 버그도 포함) -> 내 추가안과 피드백 문서 -> 이번 티켓 안까지
> 한번 순차적으로 둘러보면서 빠진게 있나 확인해.
> 또한 내 저번 지시 - 각 단계마다 플레이어의 적법한 입력이 버그를 만들지 않는지 전수조사 잘 수행했어? 그것도 반영해서 이번 티켓 작성하자.

**확정된 것**

- **플레이어 입력 전수조사는 상시 규율이다.** 게임 티켓의 완료 판정에는 「그 페이즈(게임 진행 단계)에서
  화면에 눌리는 상태로 존재하는 모든 버튼·칸을 눌렀을 때 무엇이 일어나는가」가 포함된다.
  **조용히 아무 일도 일어나지 않는 것은 통과가 아니라 결함이다.**
- **검증은 계층을 건너뛰지 않는다** — 지시 원문 → 사양 문서 → 코드 → 검수 결과 → 추가 지시 → 티켓의
  순서로 이어 대조하고, 어느 이음매에서 끊겼는지를 밝힌다. 뒤쪽 계층만 보면 앞 계층의 누락이 보이지 않는다.

**이 지시로 드러난 것** — 산출물은 `docs/inspection/fleet-input-audit-20260816.md`다.

- **2026-08-15 검수에는 입력 전수조사에 해당하는 절이 없었다.** 33건은 플레이 중 부딪힌 것 위주였다.
  그 검수 세션의 대화 기록은 저장돼 있지 않아 지시 원문을 복원할 수 없었다.
- 코드 전량 대조로 **신규 결함 8건(B-34~B-41)**을 찾았다. 네 건은 「적법한 입력이 되돌릴 수 없는
  손실이나 거짓 정보를 만든다」에 해당한다.
- **D-049가 확정한 카드 일곱 칸 중 `PAIRS`와 `TOUCHES` 두 칸이 배선에서 빠져 있었다.** 이번에 넣는다.

## D-052 · 2026-08-17 · 함대 격침 격침 표기 — 배 단위로 칠한다

> 격침표기 승인.

**확정** — supervisor 제안을 사용자가 승인했다. **격침은 칸 사건이 아니라 배 사건이므로 새 기호를
만들지 않고 배의 칠로 표현한다.**

- **침몰한 배의 칸은 진한 회색으로 메우고, 그 위의 빨강 ×는 그대로 둔다.**
  D-048이 정한 배의 「아주 옅은 회색」과 대비되어 어느 배가 죽었는지 한눈에 읽힌다.
- [도출] 새 표기 기호가 0개 늘어난다 — D-048의 세 기호와 D-050의 두 네모에 더 얹지 않는다.
- [도출] 격침은 **배 단위**이므로 배를 이루는 모든 칸이 함께 바뀐다. 칸 하나만 바뀌면 안 된다.
